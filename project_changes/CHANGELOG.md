# CHANGELOG - GPT Product Parser

Історія змін проєкту GPT Product Parser.

---

### [2025-12-12 11:02]
**Змінені файли:**
- app/parser.py
- project_changes/CHANGELOG.md

**Тип змін:** fixed

**Короткий опис:**
- Виправлено пошук “товарів” у вибраних категоріях: додано фільтрацію, щоб у результати не потрапляли категорії (якщо GPT повернув посилання на категорії замість товарів)
- Додано авто-очищення вже доданих помилкових записів (тільки `from_category_discovery=true`), якщо їх URL збігається з URL категорій конкурента

**Причина змін:**
- Після запуску “Знайти товари у вибраних категоріях” у список додавалися категорії, а не товари; потрібно гарантувати, що тут зберігаються саме товари

### [2025-12-12 10:22]
**Змінені файли:**
- app/templates/index.html
- app/static/main.js
- app/static/product.js
- app/static/tasks.js
- app/main.py
- app/models.py
- app/parser.py
- project_changes/CHANGELOG.md

**Тип змін:** updated

**Короткий опис:**
- Модальне вікно “Додати товар” спрощено до введення лише URL та кнопки “Зберегти і спарсити”
- Після додавання товару автоматично запускається повний парсинг (як кнопка “Парсинг всіх даних” на сторінці товару)
- Повний парсинг уніфіковано через фонову задачу `/tasks/parse_product_full/{product_id}`, щоб уникнути дублювання логіки

**Причина змін:**
- Спрощення UX додавання товару та запуску повного парсингу в один крок
- Використання одного механізму парсингу для однакової поведінки в різних частинах UI

## [2025-01-23]

### Змінені файли:
- `app/templates/product.html`
- `app/templates/characteristics.html`
- `app/templates/index.html`
- `app/templates/category.html`
- `app/templates/competitor.html`
- `app/templates/competitors.html`
- `app/templates/settings.html`
- `app/static/characteristics.js`
- `app/static/product.js`
- `project_changes/CHANGELOG.md`

### Тип змін: updated

### Опис:
Додано новий тип характеристики "Бренд" та видалено блок "Фото" з модальних вікон редагування характеристики та значення характеристики у всіх шаблонах.

**Зміни:**

1. **Додано тип характеристики "Бренд":**
   - Додано опцію "Бренд" у всі HTML шаблони (product.html, characteristics.html, index.html, category.html, competitor.html, competitors.html, settings.html)
   - Тип "Бренд" працює як тип "Вибір", але з можливістю додавання фото для кожного варіанту
   - Додано поле для введення URL фото при додаванні варіантів (показується тільки для типу "Бренд")
   - Додано поле для варіантів (`characteristicChoicesContainer`) у всі шаблони, де його не було
   - Оновлено JavaScript для обробки типу "brand" з `brand_choices` (масив об'єктів {name, photo_url})
   - При збереженні характеристики типу "brand" відправляється `brand_choices` замість `choices`
   - У dropdown для вибору варіантів типу "brand" відображається фото кожного варіанту

2. **Видалено блок "Фото":**
   - Видалено блок `<div class="mb-4">` з полем "Фото" з модального вікна редагування характеристики (`characteristicModal`) у всіх HTML шаблонах
   - Видалено блок `<div class="mb-4">` з полем "Фото" з модального вікна редагування значення характеристики (`characteristicValueModal`) у всіх HTML шаблонах
   - Оновлено JavaScript код: видалено встановлення значення поля фото при редагуванні, при збереженні характеристики та значення фото встановлюється як `null`

**Причина змін:**
- Додавання типу "Бренд" для характеристик, які потребують фото для кожного варіанту (наприклад, логотипи брендів)
- Спрощення інтерфейсу - видалення непотрібного поля "Фото" з карток характеристик для всіх типів характеристик

**Результат:**
- Можна створювати характеристики типу "Бренд" з фото для кожного варіанту
- У dropdown для вибору варіантів типу "Бренд" відображається фото кожного варіанту
- Модальне вікно редагування характеристики тепер не містить поля "Фото" (окрім поля для фото варіантів типу "Бренд")
- Модальне вікно редагування значення характеристики тепер містить тільки поле для введення значення

**Зміни:**
- Видалено блок `<div class="mb-4">` з полем "Фото" з модального вікна редагування характеристики (`characteristicModal`) у всіх HTML шаблонах
- Видалено блок `<div class="mb-4">` з полем "Фото" з модального вікна редагування значення характеристики (`characteristicValueModal`) у всіх HTML шаблонах
- Оновлено JavaScript код: видалено встановлення значення поля фото при редагуванні, при збереженні характеристики та значення фото встановлюється як `null`

**Причина змін:**
Спрощення інтерфейсу - видалення непотрібного поля "Фото" з карток характеристик для всіх типів характеристик (Текст, Число, Вибір, Варіація).

**Результат:**
- Модальне вікно редагування характеристики тепер не містить поля "Фото"
- Модальне вікно редагування значення характеристики тепер містить тільки поле для введення значення
- Блок "Фото" більше не відображається у картках характеристик для всіх типів

---

## [2025-01-23]

### Змінені файли:
- `run.py`
- `project_changes/CHANGELOG.md`

### Тип змін: updated

### Опис:
Виправлено помилку кодування Unicode у файлі `run.py` - замінено емодзі на звичайний текст для сумісності з кодуванням терміналу Windows (CP1251).

**Зміни:**
- Замінено емодзі ракети (🚀) на ">>>" у повідомленні про запуск сервера
- Замінено емодзі телефону (📱) на ">>>" у повідомленні про автоматичне відкриття браузера
- Замінено емодзі глобуса (🌐) на ">>>" у повідомленні про ручне відкриття

**Причина змін:**
Термінал Windows використовує кодування CP1251, яке не підтримує емодзі Unicode, що призводило до помилки `UnicodeEncodeError` та неможливості запустити сервер.

**Результат:**
- Програма тепер успішно запускається без помилок кодування
- Повідомлення відображаються коректно в терміналі Windows

---

## [2025-01-22 22:00]

### Змінені файли:
- `app/gpt_client.py`
- `app/parser.py`
- `project_changes/CHANGELOG.md`

### Тип змін: updated

### Опис:
Виправлено обробку помилки 404 у функціях `parse_first_time` та `parse_update` - тепер `ProductNotFoundError` не обгортається в загальний `Exception`.

**Зміни:**

1. **Оновлено `parse_first_time` в `gpt_client.py`:**
   - Додано окрему обробку `ProductNotFoundError` перед загальним `Exception`
   - Виняток прокидуються далі без обгортання

2. **Оновлено `parse_update` в `gpt_client.py`:**
   - Додано окрему обробку `ProductNotFoundError` перед загальним `Exception`
   - Виняток прокидуються далі без обгортання

3. **Додано логування в `parse_selected_products`:**
   - Додано логування для діагностики обробки статусу "disabled_by_competitor"
   - Додано логування в `save_result` для відстеження збереження статусу

**Результат:**
- Товари з помилкою 404 тепер правильно отримують статус "disabled_by_competitor" при парсингу вибраних товарів
- Виняток `ProductNotFoundError` правильно обробляється на всіх рівнях
- Додано детальне логування для діагностики проблем

---

## [2025-01-22 21:45]

### Змінені файли:
- `app/static/main.js`
- `project_changes/CHANGELOG.md`

### Тип змін: updated

### Опис:
Додано візуальне затемнення рядків товарів зі статусом "error" та "disabled_by_competitor" для кращого виділення проблемних товарів.

**Зміни:**

1. **Оновлено відображення рядків товарів:**
   - Товари зі статусом "error" відображаються з opacity 50% та сірим фоном
   - Товари зі статусом "disabled_by_competitor" відображаються з opacity 60% та помаранчевим фоном
   - Інші товари відображаються з нормальною прозорістю

**Результат:**
- Товари з помилками та вимкнені конкурентом візуально виділяються затемненням
- Легше знаходити проблемні товари в списку

---

## [2025-01-22 21:30]

### Змінені файли:
- `app/gpt_client.py`
- `app/parser.py`
- `app/main.py`
- `app/models.py`
- `app/templates/index.html`
- `app/static/main.js`
- `project_changes/CHANGELOG.md`

### Тип змін: updated

### Опис:
Додано автоматичне визначення товарів, вимкнених конкурентом (404 помилка) та встановлення статусу "disabled_by_competitor".

**Зміни:**

1. **Додано клас `ProductNotFoundError` в `gpt_client.py`:**
   - Спеціальний виняток для випадку, коли товар не знайдено на сайті (404)
   - Викидається в `_fetch_page_content` при отриманні 404 помилки

2. **Оновлено обробку помилок в `parse_product` та `parse_product_full`:**
   - Додано обробку `ProductNotFoundError`
   - При отриманні 404 повертається статус "disabled_by_competitor" замість помилки

3. **Оновлено `save_result` для збереження статусу:**
   - Додано обробку статусу "disabled_by_competitor"
   - При збереженні цього статусу додається лог про вимкнення товару

4. **Оновлено моделі:**
   - Додано статус "disabled_by_competitor" в коментар до моделі Product

5. **Оновлено UI:**
   - Додано відображення статусу "Вимкнений конкурентом" з помаранчевим бейджем
   - Додано опцію "Вимкнений конкурентом" у фільтрі статусів

6. **Оновлено фонові задачі парсингу:**
   - `parse_single_product`, `parse_all_products`, `parse_filtered_products`, `parse_selected_products` тепер правильно обробляють статус "disabled_by_competitor"
   - Статус "disabled_by_competitor" вважається успішним результатом, а не помилкою

**Результат:**
- Товари, які більше не існують на сайті конкурента (404), автоматично отримують статус "disabled_by_competitor"
- Статус відображається в списку товарів з помаранчевим бейджем
- Можна фільтрувати товари за статусом "Вимкнений конкурентом"
- В логах товару зберігається інформація про вимкнення

---

## [2025-01-22 21:15]

### Змінені файли:
- `app/static/main.js`
- `project_changes/CHANGELOG.md`

### Тип змін: updated

### Опис:
Виправлено відображення конкурента та категорій у списку товарів - тепер завжди використовується назва з бази даних конкурентів.

**Зміни:**

1. **Покращено пошук конкурента за назвою:**
   - Додано додатковий пошук конкурента за `competitor_name`, навіть якщо він не знайдено через `competitor_id` або `category_path`
   - Завжди використовується коротка назва з бази даних конкурентів, а не повна назва з `product.competitor_name`
   - Якщо конкурент знайдено за назвою, категорії також стають клікабельними

2. **Додано обробку категорії "Головна" в `renderCategoryBreadcrumbForList`:**
   - Категорія "Головна" показується як текст без посилання (оскільки це не реальна категорія в дереві)
   - Інші категорії залишаються клікабельними посиланнями

**Результат:**
- Конкурент тепер правильно відображається у всіх товарах (коротка назва "Кентавр" замість повної)
- Категорії правильно відображаються як клікабельні посилання (крім "Головна")
- Товари, які раніше показували "-", тепер правильно відображають конкурента та категорії

---

## [2025-01-22 21:00]

### Змінені файли:
- `app/static/product.js`
- `project_changes/CHANGELOG.md`

### Тип змін: updated

### Опис:
Виправлено проблеми з відображенням конкурента та категорій на сторінці товару.

**Зміни:**

1. **Покращено обробку помилок:**
   - Додано перевірки на null для елементів DOM
   - Додано детальне логування для діагностики проблем
   - Покращено обробку випадків, коли конкурент не знайдено

2. **Виправлено логіку пошуку конкурента:**
   - Послідовність пошуку: `competitor_id` → `category_path` → `competitor_name`
   - Кожен крок виконується тільки якщо попередній не дав результату
   - Додано перевірки статусу відповідей API

3. **Покращено рендеринг breadcrumb категорій:**
   - Додано обробку категорії "Головна" (показується як текст без посилання)
   - Додано детальне логування для діагностики проблем з пошуком категорій
   - Покращено обробку випадків, коли категорія не знайдена в дереві

**Результат:**
- Конкурент тепер правильно відображається на сторінці товару
- Категорії відображаються як клікабельні посилання (крім "Головна")
- Додано детальне логування для діагностики проблем

---

## [2025-01-22 20:30]

### Змінені файли:
- `app/templates/index.html`
- `app/static/main.js`
- `project_changes/CHANGELOG.md`

### Тип змін: updated

### Опис:
Додано відображення конкурента та категорій у списку всіх товарів з тією ж логікою, що й на сторінці товару.

**Зміни:**

1. **Оновлено таблицю товарів:**
   - Додано колонку "Конкурент" після "Назва товару"
   - Додано колонку "Категорії" після "Конкурент"
   - Оновлено colspan для повідомлень про завантаження та помилки

2. **Додано функції в `main.js`:**
   - `findCompetitorByCategory()` - пошук конкурента за категорією
   - `getCompetitorForProduct()` - отримання конкурента для товару з кешуванням
   - `renderCategoryBreadcrumbForList()` - рендеринг breadcrumb категорій для списку
   - `escapeHtml()` - екранування HTML

3. **Оновлено функцію `displayProducts()`:**
   - Тепер асинхронно завантажує конкурентів та категорії для кожного товару
   - Використовує ту саму логіку визначення конкурента, що й на сторінці товару
   - Відображає конкурента як клікабельне посилання на картку конкурента
   - Відображає категорії як інтерактивний breadcrumb з клікабельними посиланнями

**Результат:**
- У списку всіх товарів правильно відображається конкурент та категорії
- Назва конкурента є клікабельною кнопкою переходу в картку конкурента
- Категорії відображаються як інтерактивний breadcrumb з клікабельними посиланнями
- Логіка визначення конкурента однакова на сторінці товару та в списку товарів

---

## [2025-01-22 20:00]

### Змінені файли:
- `app/main.py`
- `app/static/product.js`
- `project_changes/CHANGELOG.md`

### Тип змін: updated

### Опис:
Додано автоматичне визначення конкурента за категорією товару та зроблено breadcrumb категорій інтерактивним.

**Зміни:**

1. **Додано API endpoint `/competitors/by_category`:**
   - Шукає конкурента за `category_path` товару
   - Рекурсивно перевіряє категорії в дереві категорій конкурента
   - Підтримує частковий збіг назв категорій

2. **Оновлено логіку визначення конкурента в `product.js`:**
   - Пріоритет визначення: `competitor_id` → `category_path` → `competitor_name`
   - Якщо немає `competitor_id`, система шукає конкурента за категорією товару
   - Назва конкурента відображається як клікабельне посилання на картку конкурента

3. **Оновлено breadcrumb категорій:**
   - Використовує знайдений `competitor_id` для створення посилань
   - Всі категорії в breadcrumb є клікабельними посиланнями
   - При кліку на категорію відкривається сторінка цієї категорії

**Результат:**
- Система автоматично визначає конкурента за категорією товару
- Назва конкурента є клікабельною кнопкою переходу в картку конкурента
- Breadcrumb категорій повністю інтерактивний - кожна категорія веде на свою сторінку

---

## [2025-01-22 18:30]

### Змінені файли:
- `app/templates/product.html`
- `app/static/product.js`
- `project_changes/CHANGELOG.md`

### Тип змін: updated / removed

### Опис:
Оновлено структуру сторінки товару: видалено блок "Історія цін", оновлено блоки "Таблиця історії" та "Логи парсингу", перенесено кнопки парсингу.

**Зміни:**

1. **Видалено блок "Історія цін":**
   - Видалено блок з графіком історії цін
   - Прибрано виклик функції `drawPriceChart`

2. **Оновлено блок "Таблиця історії":**
   - Тепер показує лише останні 3 записи
   - Додано кнопку "Показати всю історію" для відкриття модального вікна
   - Переміщено блок перед блоком "Логи парсингу"

3. **Оновлено блок "Логи парсингу":**
   - Тепер показує лише останні 3 записи
   - Додано кнопку "Показати всі логи" для відкриття модального вікна

4. **Додано модальні вікна:**
   - Модальне вікно для повної історії з кнопкою "Закрити"
   - Модальне вікно для всіх логів з кнопкою "Закрити"
   - Модальні вікна закриваються при кліку поза ними або на кнопку закриття

5. **Перенесено кнопки парсингу:**
   - Кнопки "Спарсити зараз" та "Парсинг всіх даних" перенесено в блок заголовка (headerBlock)
   - Прогрес-бар задачі також перенесено в headerBlock
   - Видалено блок "Парсинг та керування"

**Результат:**
- Сторінка товару стала компактнішою
- Історія та логи показують лише останні 3 записи для швидкого перегляду
- Повна історія та логи доступні через модальні вікна
- Кнопки парсингу розміщені в зручному місці

---

## [2025-01-22 18:15]

### Змінені файли:
- `app/static/characteristics.js`
- `project_changes/CHANGELOG.md`

### Тип змін: updated

### Опис:
Перероблено відображення характеристик типу "variation" на multi-select з тегами.

**Зміни:**
1. Замінено checkboxes на multi-select з тегами для типу "variation"
2. Додано поле вводу з dropdown списком варіантів
3. Обрані варіанти відображаються як теги з хрестиком для видалення
4. Обрані варіанти підсвічуються сірим кольором в dropdown списку
5. Додано можливість видаляти варіанти через хрестик на тегах
6. Додано фільтрацію варіантів при введенні тексту в поле
7. Автоматичне оновлення відображення після додавання/видалення варіантів

**Результат:**
- Характеристики типу "variation" тепер відображаються як multi-select з тегами
- Покращено UX для роботи з множинним вибором варіантів
- Візуально зрозуміло, які варіанти обрані

---

## [2025-01-22 18:00]

### Змінені файли:
- `app/static/characteristics.js`
- `project_changes/CHANGELOG.md`

### Тип змін: updated

### Опис:
Додано поле вводу для характеристик типу "text" замість кнопки "Редагувати".

**Зміни:**
1. Для характеристик типу "text" тепер відображається поле вводу типу `text` замість кнопки "Редагувати"
2. Поле автоматично зберігає значення при втраті фокусу або натисканні Enter
3. Прибрано кнопку "Редагувати" для типу "text", оскільки значення можна редагувати безпосередньо в полі
4. Додано обробник подій для збереження значення текстового поля

**Результат:**
- Характеристики типу "text" тепер мають поле вводу для тексту без кнопок
- Значення автоматично зберігається при зміні
- Покращено UX для роботи з текстовими характеристиками

---

## [2025-01-22 17:45]

### Змінені файли:
- `app/static/characteristics.js`
- `project_changes/CHANGELOG.md`

### Тип змін: updated

### Опис:
Додано поле вводу для характеристик типу "number" замість кнопки "Редагувати".

**Зміни:**
1. Для характеристик типу "number" тепер відображається поле вводу типу `number` замість кнопки "Редагувати"
2. Поле автоматично зберігає значення при втраті фокусу або натисканні Enter
3. Поле показує одиницю виміру (якщо вона встановлена) в placeholder
4. Прибрано кнопку "Редагувати" для типу "number", оскільки значення можна редагувати безпосередньо в полі

**Результат:**
- Характеристики типу "number" тепер мають поле вводу для чисел без dropdown та кнопок
- Значення автоматично зберігається при зміні
- Покращено UX для роботи з числовими характеристиками

---

## [2025-01-22 17:30]

### Змінені файли:
- `app/static/characteristics.js`
- `app/templates/product.html`
- `app/templates/characteristics.html`
- `project_changes/CHANGELOG.md`

### Тип змін: fixed / updated

### Опис:
Виправлено структуру відображення характеристик та змінено поле пріоритету на number input.

**Проблеми:**
1. Характеристики відображалися неправильно - назви були над полями замість навпроти них
2. Поле пріоритету було обмежене вибором зі списку замість вільного введення числа

**Виправлення:**
1. Виправлено структуру відображення характеристик:
   - Прибрано зайвий `<div class="mt-2">` для variation типу
   - Змінено `items-start` на `items-center` для choice типу, щоб поля були вирівняні по центру
   - Характеристики тепер відображаються одне під одним з назвами навпроти полів
2. Змінено поле пріоритету з `<select>` на `<input type="number">`:
   - Тепер можна вводити будь-яке число замість обмеженого вибору
   - Додано placeholder з підказкою
   - Змінено в обох шаблонах: `product.html` та `characteristics.html`

**Результат:**
- Характеристики відображаються правильно: назви навпроти полів, одне під одним
- Пріоритет можна встановлювати будь-яким числом, а не лише з обмеженого списку

---

## [2025-01-22 17:15]

### Змінені файли:
- `app/static/characteristics.js`
- `app/templates/characteristics.html`
- `project_changes/CHANGELOG.md`

### Тип змін: fixed

### Опис:
Виправлено проблеми з кнопками редагування груп характеристик та структурою відображення характеристик.

**Проблеми:**
1. Кнопки редагування груп характеристик не працювали
2. Кнопка "Додати групу характеристик" не працювала
3. Характеристики не відображалися правильно (не йшли списком одне під одним)

**Виправлення:**
1. Додано поле пріоритету в модальне вікно редагування групи в `characteristics.html`
2. Виправлено структуру HTML для відображення характеристик - тепер вони правильно відображаються одне під одним
3. Виправлено закриття div-ів для choice типу характеристик

**Результат:**
- Кнопки редагування груп працюють правильно
- Кнопка "Додати групу характеристик" працює
- Характеристики відображаються одне під одним у правильному порядку

---

## [2025-01-22 17:00]

### Змінені файли:
- `app/static/characteristics.js`
- `app/templates/product.html`
- `app/models.py`
- `app/main.py`
- `project_changes/CHANGELOG.md`

### Тип змін: updated / added

### Опис:
Оновлено структуру відображення характеристик та додано пріоритетність для груп характеристик.

**Зміни:**

1. **Структура відображення характеристик:**
   - Назва характеристики тепер відображається навпроти поля значення (в одному рядку)
   - Прибрано інформацію про тип та пріоритет з відображення (вона була зайвою)
   - Характеристики відображаються одне під одним, відсортовані за пріоритетом

2. **Пріоритетність груп характеристик:**
   - Додано поле `priority` до моделі `CharacteristicGroup` та `CharacteristicGroupAdd`
   - Додано поле пріоритету в модальне вікно редагування групи характеристик
   - Групи характеристик тепер відображаються відсортовані за пріоритетом (1 - найвищий, 2 - середній, 3 - найнижчий)
   - Оновлено API endpoints для збереження пріоритету групи

**Результат:**
- Характеристики відображаються в зручному форматі: назва навпроти поля значення
- Групи характеристик відображаються в правильному порядку за пріоритетом
- Можна встановлювати пріоритет для груп характеристик через модальне вікно

---

## [2025-01-22 16:45]

### Змінені файли:
- `app/static/characteristics.js`
- `project_changes/CHANGELOG.md`

### Тип змін: fixed

### Опис:
Виправлено відображення кнопки "Додати" для полів `char-searchable-input`.

**Проблема:**
Користувач міг вводити значення в поле `char-searchable-input`, але кнопка "Додати" не з'являлась для збереження значення у варіантах характеристик.

**Виправлення:**
1. Покращено знаходження кнопки "Додати" через батьківський контейнер
2. Виправлено логіку показу кнопки при введенні тексту:
   - Фільтрація реальних опцій (виключення повідомлень про відсутність варіантів)
   - Правильна перевірка наявності значення в списку варіантів
3. Додано обробники подій `keyup` та `focus` для кращої взаємодії
4. Додано перевірки на наявність кнопки перед додаванням обробників подій

---

## [2025-01-22 05:00]

### Змінені файли:
- `app/static/characteristics.js`
- `project_changes/CHANGELOG.md`

### Тип змін: fixed

### Опис:
Виправлено проблему з додаванням варіантів характеристик, коли немає жодного варіанту, та помилку "productId is not defined".

**Проблема:**
1. Коли у характеристики типу `choice` немає жодного варіанту, показувався manual input замість searchable select, що не дозволяло додавати нові варіанти з картки товару
2. Виникала помилка "productId is not defined" при спробі додати значення, бо `currentProductId` використовувався без перевірки

**Виправлення:**
1. Змінено логіку відображення для choice типу:
   - Тепер завжди показується searchable select, навіть якщо немає жодного варіанту
   - Якщо варіантів немає, в dropdown показується повідомлення "Варіанти відсутні. Введіть значення та натисніть 'Додати'"
   - Placeholder змінюється залежно від наявності варіантів
   - Кнопка "Додати" автоматично показується, якщо введено значення і немає варіантів
2. Виправлено всі місця, де використовується `currentProductId`:
   - Додано перевірку та використання `getProductIdFromUrl()` як fallback
   - Всі виклики `saveCharacteristicValueFromInput()` тепер перевіряють наявність `productId` перед викликом
   - Додано перевірку для обробників подій checkbox (variation тип)

**Результат:**
- Тепер можна додавати варіанти характеристик з картки товару, навіть якщо немає жодного варіанту
- Помилка "productId is not defined" більше не виникає
- Кнопка "Додати" автоматично з'являється при введенні значення, якщо варіантів немає

---

## [2025-01-22 04:45]

### Змінені файли:
- `app/static/characteristics.js`
- `app/main.py`
- `project_changes/CHANGELOG.md`

### Тип змін: fixed / added

### Опис:
Виправлено проблему з додаванням значень характеристик через manual input та додано функціонал для очищення варіантів характеристик.

**Проблема 1:**
В полі `char-manual-input` не працювало додавання характеристик, якщо не додано жодної характеристики (не було productId).

**Проблема 2:**
Потрібно було очистити варіанти характеристик з карток товарів для конкретних товарів.

**Виправлення:**
1. Виправлено обробник подій для `char-manual-input`:
   - Додано перевірку наявності `productId` перед збереженням значення
   - Якщо `productId` відсутній, показується повідомлення користувачу
   - Використовується `getProductIdFromUrl()` для отримання ID товару з URL
2. Додано новий API endpoint `/products/{product_id}/characteristics/clear-choices`:
   - Очищає всі значення характеристик типу `choice` та `variation` для вказаного товару
   - Повертає кількість видалених значень
3. Очищено варіанти характеристик для товарів:
   - Мотоблок Кентавр МБ1012Д (ID: a351f2ed-9700-4848-8fca-3d007d4fd6f3) - видалено 2 значення
   - Мотоблок Кентавр МБ 1080Д (ID: 4c98ad22-a767-466b-9c1f-133f0306ecc5) - видалено 2 значення

**Результат:**
- Тепер можна додавати значення характеристик через manual input в будь-який момент (навіть якщо не додано жодної характеристики)
- Додано можливість очищення варіантів характеристик для товарів через API
- Очищено варіанти характеристик для вказаних товарів

---

## [2025-01-22 04:30]

### Змінені файли:
- `app/static/characteristics.js`
- `project_changes/CHANGELOG.md`

### Тип змін: fixed

### Опис:
Виправлено проблему з видаленням значення характеристики, коли залишається 0 варіантів після видалення останнього варіанту.

**Проблема:**
Коли в характеристиці був 1 варіант, він був вибраний в товарі, а потім його видалили - значення залишалося в картці товару. Коли було 2 варіанти і один видалили - все працювало правильно.

**Причина:**
Функція `clearCharacteristicValueIfMatches()` не спрацьовувала правильно для випадку, коли після видалення залишалося 0 варіантів. Також логіка порівняння значень була занадто складною і могла не спрацювати для точного співпадіння.

**Виправлення:**
1. Спрощено логіку порівняння в `clearCharacteristicValueIfMatches()`:
   - Тепер перевіряється точне співпадіння після нормалізації (видалення пробілів)
   - Додано перевірку співпадіння без урахування регістру
   - Додано детальне логування для відлагодження
2. Додано додаткову перевірку в `removeCharacteristicChoice()`:
   - Якщо після видалення залишається 0 варіантів, виконується додаткова перевірка та очищення значення
3. Додано автоматичне очищення значення при відображенні:
   - Якщо залишилося 0 варіантів, але значення не порожнє, воно автоматично очищається

**Результат:**
Тепер при видаленні останнього варіанту характеристики значення правильно видаляється з картки товару, незалежно від кількості варіантів (1 або більше).

---

## [2025-01-22 04:15]

### Змінені файли:
- `app/static/characteristics.js`
- `project_changes/CHANGELOG.md`

### Тип змін: fixed / updated

### Опис:
Виправлено дві проблеми з відображенням та синхронізацією варіантів характеристик.

**Проблема 1:**
При видаленні варіанту характеристики (наприклад, "300 см3") значення не видалялося з картки товару, якщо воно не точно співпадало з видаленим варіантом.

**Проблема 2:**
Коли характеристика має варіанти (choices.length > 0), але поточне значення не відповідає жодному варіанту (наприклад, варіант був видалений), показувався manual input замість searchable select. Також при кліку на заповнене поле не відкривався dropdown з варіантами.

**Виправлення:**
1. Покращено функцію `clearCharacteristicValueIfMatches()` - тепер перевіряється не тільки точне співпадіння, але й часткове (на випадок зайвих пробілів або незначних відмінностей)
2. Змінено логіку відображення для choice типу:
   - Якщо є варіанти (choices.length > 0), завжди показується searchable select, навіть якщо поточне значення не відповідає жодному варіанту
   - Якщо поточне значення не відповідає варіантам, воно автоматично очищається при відображенні
   - Прибрано атрибут `readonly` з input поля, щоб завжди можна було вибрати варіант
3. Покращено обробники подій для searchable input:
   - При кліку на поле завжди знімається readonly і показується dropdown з варіантами
   - Dropdown відкривається навіть якщо поле вже заповнене

**Результат:**
- При видаленні варіанту характеристики значення автоматично видаляється з картки товару
- Якщо є варіанти, завжди показується searchable select з можливістю вибору
- При кліку на поле завжди відкривається dropdown з варіантами

---

## [2025-01-22 04:00]

### Змінені файли:
- `app/static/characteristics.js`
- `project_changes/CHANGELOG.md`

### Тип змін: fixed

### Опис:
Виправлено критичну помилку, яка блокувала відображення характеристик.

**Проблема:**
Перевірка та очищення невалідних значень виконувалася всередині циклу рендерингу HTML для кожної групи окремо, що блокувало відображення характеристик.

**Виправлення:**
- Винесено перевірку значень за межі циклу рендерингу HTML
- Перевірка тепер виконується один раз для всіх характеристик перед рендерингом
- Використано `for...of` цикл замість `forEach` для правильної підтримки асинхронних операцій
- Перевірка виконується тільки якщо є `productId` (на сторінці товару)

**Результат:**
Характеристики тепер правильно відображаються на сторінці товару та на сторінці характеристик.

---

## [2025-01-22 04:00]

### Змінені файли:
- `app/static/characteristics.js`
- `project_changes/CHANGELOG.md`

### Тип змін: fixed

### Опис:
Виправлено проблему з відображенням характеристик - видалено блокування рендерингу через перевірку значень.

**Проблема:**
Перевірка значень характеристик виконувалася перед рендерингом HTML, що могло блокувати відображення характеристик, особливо на сторінці характеристик, де немає productId.

**Виправлення:**
- Видалено перевірку значень перед рендерингом HTML в функції `displayCharacteristics()`
- Перевірка значень тепер виконується тільки при видаленні варіанту через функцію `removeCharacteristicChoice()`
- Характеристики тепер відображаються одразу без блокування

**Результат:**
Характеристики та групи тепер правильно завантажуються та відображаються на всіх сторінках.

---

## [2025-01-22 03:45]

### Змінені файли:
- `app/static/characteristics.js`
- `project_changes/CHANGELOG.md`

### Тип змін: fixed

### Опис:
Виправлено проблему з завантаженням характеристик та груп на сторінці характеристик.

**Проблема:**
Після додавання автоматичної перевірки значень в функції `displayCharacteristics()`, використання `await` всередині `forEach` циклу призводило до блокування завантаження характеристик та груп.

**Виправлення:**
- Винесено перевірку та очищення невалідних значень за межі циклу рендерингу HTML
- Замінено `forEach` на `for...of` цикл для правильної підтримки асинхронних операцій
- Додано перевірку `if (productId)` перед виконанням перевірки значень (перевірка виконується тільки на сторінці товару, а не на сторінці характеристик)
- Перевірка значень виконується перед рендерингом HTML, що забезпечує правильне оновлення даних

**Результат:**
Тепер характеристики та групи правильно завантажуються на сторінці характеристик, а автоматична перевірка значень працює тільки на сторінці товару.

---

## [2025-01-22 03:30]

### Змінені файли:
- `app/static/characteristics.js`
- `project_changes/CHANGELOG.md`

### Тип змін: updated

### Опис:
Виправлено та покращено синхронізацію видалення варіантів характеристик між модальним вікном редагування та карткою товару.

**Зміни:**
- Модифіковано функцію `removeCharacteristicChoice()` для автоматичного оновлення характеристики на сервері одразу після видалення варіанту
- Додано функцію `clearCharacteristicValueIfMatches()` для автоматичного очищення значень у картці товару, які відповідають видаленому варіанту
- Додано автоматичну перевірку в функцію `displayCharacteristics()` для очищення значень, які не відповідають жодному варіанту зі списку
- При видаленні варіанту (наприклад, "200 см3 ×") автоматично:
  - Спочатку очищається значення в картці товару, якщо воно відповідає видаленому варіанту
  - Оновлюється характеристика на сервері з новим списком варіантів
  - Перезавантажуються характеристики в картці товару для оновлення списку варіантів
- При відображенні характеристик автоматично перевіряється, чи значення відповідає варіантам зі списку, і якщо ні - очищається
- Підтримка всіх типів характеристик: choice (одне значення), variation (кілька значень через кому), manual input
- Додано детальне логування для відлагодження

**Причина змін:**
Користувач повідомив, що при видаленні варіанту характеристики "200 см3" зі списку варіантів, відповідний input з цим значенням все ще залишався в картці товару. Тепер видалення варіанту автоматично синхронізується з карткою товару, а також додано автоматичну перевірку при відображенні характеристик.

---

## [2025-01-22 03:15]

### Змінені файли:
- `app/static/characteristics.js`
- `project_changes/CHANGELOG.md`

### Тип змін: updated

### Опис:
Покращено синхронізацію видалення варіантів характеристик між модальним вікном редагування та карткою товару.

**Зміни:**
- Модифіковано функцію `removeCharacteristicChoice()` для автоматичного оновлення характеристики на сервері одразу після видалення варіанту
- Додано функцію `clearCharacteristicValueIfMatches()` для автоматичного очищення значень у картці товару, які відповідають видаленому варіанту
- При видаленні варіанту (наприклад, "100 см3 ×") автоматично:
  - Оновлюється характеристика на сервері з новим списком варіантів
  - Очищаються значення в картці товару, які відповідають видаленому варіанту (для всіх типів: choice, variation, manual input)
  - Перезавантажуються характеристики в картці товару для оновлення списку варіантів
- Підтримка всіх типів характеристик: choice (одне значення), variation (кілька значень через кому), manual input

**Причина змін:**
Користувач повідомив, що при видаленні варіанту характеристики в модальному вікні (наприклад, "100 см3 ×"), відповідний input з цим значенням не зникав у картці товару. Тепер видалення варіанту автоматично синхронізується з карткою товару.

---

## [2025-01-22 03:00]

### Змінені файли:
- `app/static/characteristics.js`

### Тип змін: updated

### Опис:
Додано синхронізацію видалення варіантів характеристик між модальним вікном редагування та карткою товару.

**Зміни:**
- Модифіковано функцію `removeCharacteristicChoice()` для автоматичного видалення відповідних значень у картці товару при видаленні варіанту характеристики
- При видаленні варіанту (наприклад, "100 см3 ×") автоматично:
  - Очищаються input поля з класом `char-manual-input`, які мають відповідне значення
  - Очищаються searchable input поля (для choice типу)
  - Знімаються галочки з checkbox полів (для variation типу)
  - Оновлюються значення на сервері (видаляється значення з бази даних)
- Функція експортована глобально через `window.removeCharacteristicChoice` для використання в onclick обробниках

**Причина змін:**
Забезпечення консистентності даних між списком варіантів характеристики та значеннями, встановленими для товару. Коли варіант видаляється зі списку, він також має автоматично видалятися з картки товару.

---

## [2025-01-22 02:30]

### Змінені файли:
- `app/templates/index.html`
- `app/templates/settings.html`
- `app/templates/competitors.html`
- `app/templates/competitor.html`
- `app/templates/category.html`
- `app/templates/product.html`
- `app/static/characteristics.js` (створено)
- `app/static/product.js`
- `project_changes/CHANGELOG.md`

### Тип змін: updated / added

### Опис:
Додано кнопку "Характеристики" до header на всіх сторінках проєкту та створено глобальний JavaScript файл для роботи з характеристиками.

**Зміни в шаблонах:**
- Додано кнопку "Характеристики" (`id="openCharacteristicsBtn"`) до навігації на всіх сторінках:
  - `index.html` (Товари)
  - `settings.html` (Налаштування)
  - `competitors.html` (Конкуренти)
  - `competitor.html` (Картка конкурента)
  - `category.html` (Категорія)
  - `product.html` (Картка товару)
- Додано модальні вікна для роботи з характеристиками на всіх сторінках:
  - Модальне вікно для перегляду характеристик (`characteristicsModal`)
  - Модальне вікно для створення/редагування груп (`groupModal`)
  - Модальне вікно для створення/редагування характеристик (`characteristicModal`)
  - Модальне вікно для редагування значень характеристик (`characteristicValueModal`)

**Новий файл characteristics.js:**
- Створено глобальний JavaScript файл `app/static/characteristics.js` для роботи з характеристиками на всіх сторінках
- Функції:
  - `loadCharacteristics()` - завантаження характеристик для товару або загальний список
  - `loadAllCharacteristics()` - завантаження всіх характеристик (якщо не на сторінці товару)
  - `loadGroups()` - завантаження груп характеристик
  - `displayCharacteristics()` - відображення характеристик з групуванням та сортуванням
  - `displayAllCharacteristics()` - відображення всіх характеристик без прив'язки до товару
  - `openCharacteristicsModal()` / `closeCharacteristicsModal()` - управління модальним вікном
  - `openAddGroupModal()` / `saveGroup()` - створення/редагування груп
  - `openAddCharacteristicModal()` / `saveCharacteristic()` - створення/редагування характеристик
  - `editCharacteristicValue()` / `saveCharacteristicValue()` - редагування значень характеристик
  - `updateCategoryFilter()` - оновлення фільтра категорій
  - `updateGroupSelects()` - оновлення select з групами
  - `showToast()` - показ повідомлень користувачу
- Автоматичне визначення контексту (сторінка товару чи загальна сторінка)
- Підтримка пошуку та фільтрації характеристик

**Зміни в product.js:**
- Видалено дублікати функцій, які тепер використовуються з `characteristics.js`
- Підключено `characteristics.js` до сторінки товару

**Причина змін:**
Кнопка "Характеристики" має бути доступна на всіх сторінках проєкту, а не тільки на сторінці товару, для зручного доступу до управління характеристиками з будь-якої сторінки.

---

## [2025-01-22 01:00]

### Змінені файли:
- `app/models.py`
- `app/parser.py`
- `app/main.py`
- `app/templates/product.html`
- `app/static/product.js`
- `project_changes/CHANGELOG.md`

### Тип змін: added

### Опис:
Додано повну логіку для роботи з характеристиками товарів та їх групами. Зміни:

**Моделі даних (models.py):**
- Додано модель `CharacteristicGroup` для груп характеристик з полями:
  - id, name, description, category_path, created_at, updated_at
- Додано модель `Characteristic` для характеристик з полями:
  - id, name, type (text/number/choice), priority (1-3), unit, category_path, group_id, photo_url, created_at, updated_at
- Додано модель `CharacteristicValue` для значень характеристик товарів
- Додано моделі для додавання/оновлення: `CharacteristicGroupAdd`, `CharacteristicAdd`, `CharacteristicValueAdd`

**Функції для роботи з характеристиками (parser.py):**
- Додано `load_characteristics()` - завантаження бази даних характеристик з файлу `characteristics.json`
- Додано `save_characteristics()` - збереження бази даних характеристик
- Додано `get_characteristics_for_product()` - отримання характеристик для товару з урахуванням категорії
- Додано `get_product_characteristic_values()` - отримання значень характеристик для товару
- Структура БД: `{groups: [], characteristics: [], product_characteristics: {product_id: [values]}}`

**API endpoints (main.py):**
- `GET /characteristics/groups` - список груп характеристик
- `POST /characteristics/groups` - додавання групи
- `PUT /characteristics/groups/{group_id}` - оновлення групи
- `DELETE /characteristics/groups/{group_id}` - видалення групи
- `GET /characteristics` - список характеристик з фільтрами (name, category_path, group_id)
- `POST /characteristics` - додавання характеристики
- `PUT /characteristics/{char_id}` - оновлення характеристики
- `DELETE /characteristics/{char_id}` - видалення характеристики
- `GET /products/{product_id}/characteristics` - характеристики для товару
- `POST /products/{product_id}/characteristics` - додавання/оновлення значення характеристики
- `DELETE /products/{product_id}/characteristics/{char_id}` - видалення значення характеристики

**UI зміни (product.html):**
- Додано блок "Характеристики" з кнопками:
  - "➕ Додати групу характеристик"
  - "➕ Додати характеристику"
- Додано поле пошуку по назві характеристики
- Додано фільтр по категорії товару
- Додано модальне вікно для створення/редагування групи характеристик:
  - Поля: назва групи, опис, категорія товару
- Додано модальне вікно для створення/редагування характеристики:
  - Поля: назва, тип (текст/число/вибір), пріоритет (1-3), одиниця виміру, група, категорія, фото
- Додано модальне вікно для редагування значення характеристики:
  - Поля: значення, фото

**JavaScript логіка (product.js):**
- Додано `loadCharacteristics()` - завантаження характеристик для товару
- Додано `loadGroups()` - завантаження груп характеристик
- Додано `displayCharacteristics()` - відображення характеристик з групуванням за групами та сортуванням за пріоритетом
- Додано функції для відкриття/закриття модальних вікон
- Додано `saveGroup()`, `saveCharacteristic()`, `saveCharacteristicValue()` - збереження даних
- Додано фільтрацію та пошук характеристик в реальному часі
- Додано `showToast()` - показ повідомлень про успіх/помилки
- Автоматичне оновлення списку характеристик після змін

**Відображення характеристик:**
- Характеристики групуються за групами
- Під кожною групою відображаються характеристики з їх значеннями
- Сортування за пріоритетом: 1 (найвищий) → 2 (середній) → 3 (найнижчий)
- Відображення фото для характеристик типу "Колір", "Бренд"
- Відображення одиниці виміру для числових характеристик
- Можливість додавання/редагування значень характеристик для товару

**Фільтрація та пошук:**
- Пошук по назві характеристики (в реальному часі)
- Фільтрація по категорії товару
- Автоматичне оновлення списку при зміні фільтрів

**Прив'язка до категорій:**
- Групи та характеристики можуть бути прив'язані до категорій товарів
- При відображенні характеристик для товару показуються тільки ті, що прив'язані до категорії товару або без прив'язки
- Категорії вводяться через текст з розділювачем " > " (наприклад: "Електроніка > Смартфони")

### Причина змін:
Додавання повної системи управління характеристиками товарів для деталізації інформації про товари. Користувач може створювати групи характеристик, додавати характеристики з різними типами та пріоритетами, прив'язувати їх до категорій та встановлювати значення для конкретних товарів.

---

## [2025-01-22 00:25]

### Змінені файли:
- `app/gpt_client.py`
- `app/parser.py`
- `project_changes/CHANGELOG.md`

### Тип змін: updated / fixed

### Опис:
Покращено обробку помилок та додано retry логіку для парсингу категорій. Зміни:

**Збільшення таймаутів (gpt_client.py):**
- Збільшено таймаут GPT API клієнта з 60 до 120 секунд
- Збільшено таймаут для HTTP запитів при парсингу категорій до 120 секунд
- Додано retry логіку на рівні `parse_competitor_categories()`:
  - 3 спроби отримання сторінки з збільшенням таймауту
  - 3 спроби виклику GPT API з затримками між спробами
  - Exponential backoff для затримок (5s, 10s, 15s для GPT API)

**Retry логіка для HTTP запитів:**
- Метод `_fetch_page_content()` має вбудовану retry логіку
- При парсингу категорій використовується таймаут 120 секунд
- Збільшення таймауту з кожною спробою (120s, 180s, 240s)
- Детальне логування кожної спроби
- Спеціальна обробка різних типів помилок:
  - `httpx.TimeoutException` - таймаут запиту
  - `httpx.HTTPStatusError` - помилка HTTP статусу
  - `httpx.RequestError` - помилка запиту

**Retry логіка для GPT API:**
- Додано retry логіку для GPT API викликів в `parse_competitor_categories()`
- 3 спроби з затримками 5, 10, 15 секунд між спробами
- Детальне логування кожної спроби
- Інформативні повідомлення про помилки

**Покращена обробка помилок (parser.py):**
- Додано try-catch блок навколо виклику `parse_competitor_categories()`
- Детальне логування помилок з traceback
- Правильне оновлення статусу задачі при помилці
- Повідомлення про помилку зберігається в прогрес задачі

**Логування:**
- Додано детальне логування на кожному етапі:
  - Спроби отримання сторінки
  - Спроби виклику GPT API
  - Очікування між спробами
  - Успішне завершення або помилки

### Причина змін:
Користувач повідомив про помилку "Request timed out" при оновленні категорій. Потрібно було:
1. Збільшити таймаути для великих сторінок
2. Додати retry логіку для надійності
3. Покращити обробку помилок з детальним логуванням
4. Забезпечити автоматичні повторні спроби при тимчасових помилках

---

## [2025-01-22 00:25]

### Змінені файли:
- `app/gpt_client.py`
- `app/parser.py`
- `project_changes/CHANGELOG.md`

### Тип змін: updated / fixed

### Опис:
Покращено обробку помилок та додано retry логіку для парсингу категорій. Зміни:

**Збільшення таймаутів (gpt_client.py):**
- Збільшено таймаут GPT API клієнта з 60 до 120 секунд
- Збільшено таймаут для HTTP запитів при парсингу категорій до 120 секунд
- Додано retry логіку на рівні `parse_competitor_categories()`:
  - 3 спроби отримання сторінки з збільшенням таймауту
  - 3 спроби виклику GPT API з затримками між спробами
  - Exponential backoff для затримок (5s, 10s, 15s для GPT API)

**Retry логіка для HTTP запитів:**
- Метод `_fetch_page_content()` має вбудовану retry логіку
- При парсингу категорій використовується таймаут 120 секунд
- Збільшення таймауту з кожною спробою (120s, 180s, 240s)
- Детальне логування кожної спроби

**Retry логіка для GPT API:**
- Додано retry логіку для GPT API викликів в `parse_competitor_categories()`
- 3 спроби з затримками 5, 10, 15 секунд між спробами
- Детальне логування кожної спроби
- Інформативні повідомлення про помилки

**Покращена обробка помилок (parser.py):**
- Додано try-catch блок навколо виклику `parse_competitor_categories()`
- Детальне логування помилок з traceback
- Правильне оновлення статусу задачі при помилці
- Повідомлення про помилку зберігається в прогрес задачі

**Логування:**
- Додано детальне логування на кожному етапі:
  - Спроби отримання сторінки
  - Спроби виклику GPT API
  - Очікування між спробами
  - Успішне завершення або помилки

### Причина змін:
Користувач повідомив про помилку "Request timed out" при оновленні категорій. Потрібно було:
1. Збільшити таймаути для великих сторінок
2. Додати retry логіку для надійності
3. Покращити обробку помилок з детальним логуванням
4. Забезпечити автоматичні повторні спроби при тимчасових помилках

---

## [2025-01-22 00:20]

### Змінені файли:
- `app/gpt_client.py`
- `app/templates/competitor.html`
- `app/static/competitor.js`
- `app/parser.py`
- `project_changes/CHANGELOG.md`

### Тип змін: updated / removed / fixed

### Опис:
Видалено кнопку "Спарсити категорії" та виправлено помилку таймауту. Зміни:

**Видалення кнопки "Спарсити категорії" (competitor.html, competitor.js):**
- Видалено кнопку "Спарсити категорії" з HTML
- Видалено обробник події для кнопки "Спарсити категорії"
- Кнопка "Оновити категорії" тепер виконує обидві функції:
  - Якщо категорій немає - працює як парсинг (додає нові категорії)
  - Якщо категорії є - працює як оновлення (порівнює та оновлює)
- Повідомлення підтвердження змінюється залежно від наявності категорій

**Виправлення помилки таймауту (gpt_client.py):**
- Збільшено таймаут для HTTP запитів при парсингу категорій з 20 до 60 секунд
- Додано параметр `timeout` до методу `_fetch_page_content()` (за замовчуванням 30 секунд)
- Для парсингу категорій використовується таймаут 60 секунд
- Додано спеціальну обробку помилки `httpx.TimeoutException` з інформативним повідомленням

**Оновлення функції оновлення категорій (parser.py):**
- Функція `update_competitor_categories()` тепер працює навіть якщо категорій ще немає
- Якщо старих категорій немає - просто додає нові (працює як перший парсинг)
- Якщо категорії є - виконує порівняння та оновлення як раніше
- Показує відповідне повідомлення залежно від типу операції

**Покращення UX:**
- Кнопка "Оновити категорії" має синій колір (раніше була сіра) для кращої видимості
- Повідомлення підтвердження адаптується до ситуації
- Спрощено інтерфейс - одна кнопка замість двох

### Причина змін:
1. Користувач повідомив про помилку "Request timed out" при оновленні категорій
2. Користувач запропонував видалити кнопку "Спарсити категорії", оскільки кнопка "Оновити категорії" може виконувати ту саму функцію
3. Потрібно було збільшити таймаут для великих сторінок та покращити обробку помилок

---

## [2025-01-22 00:10]

### Змінені файли:
- `app/templates/competitor.html`
- `app/static/competitor.js`
- `app/main.py`
- `project_changes/CHANGELOG.md`

### Тип змін: added / updated

### Опис:
Додано кнопку видалення вибраних категорій, пошукач по категоріях та кнопку "Обрати всі". Зміни:

**Кнопка "Видалити вибране" (competitor.html, competitor.js):**
- Додано кнопку "🗑️ Видалити вибране" яка з'являється разом з кнопкою "Знайти товари"
- Кнопка показується тільки коли є вибрані категорії (галочки)
- При натисканні видаляє всі вибрані категорії з дерева
- Підтвердження перед видаленням з інформацією про кількість категорій
- Показ повідомлення про успіх після видалення
- Автоматичне оновлення дерева категорій після видалення

**Пошукач по категоріях (competitor.html, competitor.js):**
- Додано поле пошуку "🔍 Пошук по категоріях..." над деревом категорій
- Пошук працює в реальному часі (при введенні тексту)
- Фільтрує категорії по назві та URL
- Рекурсивний пошук - знаходить категорії на всіх рівнях вкладеності
- Показує категорії, які містять шуканий текст, та їх батьківські категорії
- При очищенні поля пошуку показуються всі категорії

**Кнопка "Обрати всі" (competitor.html, competitor.js):**
- Додано кнопку "Обрати всі" для швидкого вибору всіх категорій
- При натисканні проставляються галочки на всіх категоріях
- Якщо всі вже вибрані - знімаються всі галочки
- Кнопка має сірий колір для візуального розрізнення

**Endpoint для видалення категорій (main.py):**
- Додано `POST /competitors/{competitor_id}/delete_categories` для видалення категорій
- Endpoint приймає JSON з полем `category_ids` (масив ID категорій)
- Рекурсивне видалення категорій по всьому дереву
- Видаляє категорії та всі їх дочірні категорії
- Повертає кількість видалених категорій

**Оновлена логіка показу кнопок (competitor.js):**
- Функція `updateDiscoverButton()` тепер показує/ховає обидві кнопки:
  - "Знайти товари у вибраних категоріях"
  - "Видалити вибране"
- Кнопки з'являються тільки коли є вибрані категорії
- Автоматичне оновлення видимості після рендерингу дерева

**Збереження оригінальних категорій (competitor.js):**
- Додано збереження оригінальних категорій для фільтрації
- Оригінальні категорії зберігаються при першому рендерингу
- Використовуються для пошуку та відновлення після очищення пошуку

**Логіка роботи:**
1. Користувач може шукати категорії через поле пошуку
2. Може вибрати всі категорії одним кліком
3. Може видалити вибрані категорії
4. Може знайти товари у вибраних категоріях
5. Всі дії оновлюють дерево категорій автоматично

### Причина змін:
Користувач попросив:
1. Додати кнопку "Видалити вибране" яка з'являється разом з кнопкою "Знайти товари"
2. Додати пошукач по категоріях для швидкого пошуку
3. Додати кнопку "Обрати всі категорії" для проставлення галочок по всім категоріям

---

## [2025-01-21 23:55]

### Змінені файли:
- `app/main.py`
- `app/static/competitor.js`
- `project_changes/CHANGELOG.md`

### Тип змін: updated / fixed

### Опис:
Додано перевірку домену URL та покращено перевірку дублікатів при додаванні категорій вручну. Зміни:

**Перевірка домену URL (main.py):**
- Додано перевірку, чи URL категорії належить поточному конкуренту
- Порівняння доменів з урахуванням www (нормалізація доменів)
- Якщо домен не співпадає - показується помилка з деталями:
  "URL категорії належить іншому постачальнику. Домен конкурента: X, домен категорії: Y"
- Запобігає додаванню категорій від інших конкурентів

**Покращена перевірка дублікатів (main.py):**
- Додано перевірку дублікатів не тільки по ID, але й по URL
- Функція `category_exists()` тепер перевіряє обидва параметри
- Якщо категорія з таким URL вже існує - показується помилка:
  "Категорія з таким URL вже існує"
- Якщо категорія з таким ID вже існує - показується помилка:
  "Категорія з таким ID вже існує"
- Рекурсивна перевірка по всьому дереву категорій (включаючи підкатегорії)

**Покращення відображення помилок (competitor.js):**
- Помилки тепер показуються у спливаючому вікні (toast) замість alert
- Використовується функція `showToast()` для кращого UX
- Помилки мають червоний колір та іконку ❌

**Логіка перевірки:**
1. Валідація формату URL
2. Перевірка домену (чи належить поточному конкуренту)
3. Генерація ID на основі URL
4. Перевірка дублікатів по ID
5. Перевірка дублікатів по URL
6. Якщо всі перевірки пройдені - додавання категорії

### Причина змін:
Користувач повідомив, що додав категорію іншого конкурента (romb.ua замість kentavr.ua). Потрібно було:
1. Перевіряти домен URL при додаванні категорії
2. Покращити перевірку дублікатів - перевіряти не тільки по ID, але й по URL
3. Показувати зрозумілі помилки користувачу

---

## [2025-01-21 23:45]

### Змінені файли:
- `app/templates/competitor.html`
- `app/static/competitor.js`
- `app/main.py`
- `app/gpt_client.py`
- `project_changes/CHANGELOG.md`

### Тип змін: added

### Опис:
Додано можливість ручного додавання категорій конкурентів. Зміни:

**Нова кнопка та модальне вікно (competitor.html):**
- Додано кнопку "➕ Додати категорію вручну" в блок з кнопками управління
- Створено модальне вікно для додавання категорії з полями:
  - URL категорії (обов'язкове поле)
  - Назва категорії (обов'язкове поле)
- Модальне вікно має сучасний дизайн у стилі проєкту
- Додано кнопки "Додати" та "Скасувати"
- Модальне вікно закривається при кліку поза ним

**Frontend функціонал (competitor.js):**
- Додано функцію `openAddCategoryModal()` для відкриття модального вікна
- Додано функцію `closeAddCategoryModal()` для закриття модального вікна
- Додано функцію `addCategoryManually()` для відправки запиту на додавання категорії
- Валідація URL перед відправкою
- Показ повідомлення про успіх після додавання
- Автоматичне оновлення дерева категорій після додавання
- Обробники подій для всіх кнопок та форми

**Backend endpoint (main.py):**
- Додано `POST /competitors/{competitor_id}/add_category` для додавання категорії
- Endpoint приймає JSON з полями `url` та `name`
- Валідація URL та назви
- Генерація ID категорії на основі URL (slug з шляху)
- Перевірка на дублікати (категорія з таким ID не додається двічі)
- Автоматичне оновлення назви категорії через GPT (якщо є активний API ключ)
- Позначення категорії як `manual_added: true` для відстеження

**Функція парсингу назви категорії (gpt_client.py):**
- Додано метод `parse_category_name(category_url)` для парсингу назви категорії з URL
- GPT аналізує HTML сторінки категорії та витягує назву з:
  - Заголовків сторінки (h1, h2)
  - Title сторінки
  - Breadcrumb навігації
  - Мета-тегів
- Якщо не вдалося витягти через GPT, використовується fallback на основі URL
- Автоматичне оновлення назви категорії після додавання (якщо є API ключ)

**Логіка роботи:**
1. Користувач натискає "Додати категорію вручну"
2. Відкривається модальне вікно з полями URL та назва
3. Користувач вводить URL та назву категорії
4. При натисканні "Додати" відправляється запит на сервер
5. Сервер додає категорію до дерева категорій конкурента
6. Якщо є активний API ключ, сервер автоматично парсить назву категорії з URL через GPT
7. Назва категорії оновлюється на правильну (як на сайті конкурента)
8. Дерево категорій оновлюється на сторінці
9. Показується повідомлення про успіх

**Візуальні покращення:**
- Кнопка має зелений колір для візуального розрізнення
- Модальне вікно має backdrop з затемненням
- Поля форми мають правильну валідацію та підказки
- Показ повідомлень про помилки та успіх

### Причина змін:
Користувач попросив додати можливість ручного додавання категорій, коли автоматичний парсинг не знаходить всі категорії. При додаванні категорії вручну потрібно лише вставити URL та назвати її, а при парсингу категорії GPT автоматично перейменує її на правильну назву з сайту конкурента.

---

## [2025-01-21 23:30]

### Змінені файли:
- `app/gpt_client.py`
- `app/parser.py`
- `app/main.py`
- `app/static/tasks.js`
- `app/static/competitor.js`
- `project_changes/CHANGELOG.md`

### Тип змін: updated / added

### Опис:
Покращено систему парсингу та оновлення категорій конкурентів. Зміни:

**Покращення промптів для пошуку категорій (gpt_client.py):**
- Значно розширено інструкції для GPT з детальним описом ВСІХ місць пошуку категорій
- Додано обов'язкову перевірку 9 різних місць:
  1. Головне меню навігації (header, nav, menu) - з mega-menu
  2. Footer (нижня частина сторінки) - з колонками та sitemap
  3. Sidebar (бокова панель) - з фільтрами категорій
  4. Мобільне меню - з прихованими меню
  5. Каталог товарів - з блоками категорій
  6. Dropdown меню - з усіма рівнями вкладеності
  7. Sitemap - HTML та XML
  8. Блоки з посиланнями - всі <a> теги
  9. Додаткові місця - баннери, блоки рекомендацій
- Підкреслено критичну важливість знаходження ВСІХ категорій без винятку
- Додано інструкції для перевірки всіх рівнів вкладеності (батьківські, дочірні, внучата, правнуки)

**Нова функція оновлення категорій (parser.py):**
- Додано функцію `update_competitor_categories()` для розумного оновлення категорій
- Функція порівнює старі та нові категорії за ID, URL та назвою
- Логіка оновлення:
  - Якщо категорія знайдена - залишається без змін (оновлюються тільки дані)
  - Якщо категорія не знайдена - ставиться помітка `needs_manual_check: true` для ручної перевірки
  - Якщо знаходиться нова категорія - вона додається до дерева
- Рекурсивна обробка всіх рівнів вкладеності категорій
- Збір статистики: знайдено, не знайдено, нових категорій
- Детальне повідомлення зі статистикою для користувача

**Новий endpoint для оновлення (main.py):**
- Додано `POST /tasks/update_categories/{competitor_id}` для створення задачі оновлення
- Endpoint створює фонову задачу з типом "update_categories"
- Додано імпорт функції `update_competitor_categories`

**Оновлення frontend (tasks.js, competitor.js):**
- Додано функцію `createUpdateCategoriesTask()` для створення задачі оновлення
- Кнопка "Оновити категорії" тепер використовує окремий endpoint для оновлення
- Додано спеціальну обробку задачі `update_categories` в `handleTaskComplete()`
- Показ статистики оновлення у спливаючому вікні (toast)
- Оновлення дерева категорій після завершення оновлення
- Візуальне виділення категорій з поміткою "needs_manual_check":
  - Жовтий фон (`bg-yellow-50`)
  - Жовта ліва межа (`border-l-4 border-yellow-400`)
  - Іконка попередження та текст "⚠️ Потрібна перевірка"

**Візуальні покращення (competitor.js):**
- Категорії з поміткою `needs_manual_check` відображаються з жовтим виділенням
- Додано текст "⚠️ Потрібна перевірка" біля назви категорії
- Зміна кольору тексту на жовтий для незнайдених категорій

### Причина змін:
Користувач повідомив, що кнопка "Спарсити категорії" не працює правильно, оскільки раніше вже були спаршені категорії, але не всі додалися. Потрібно було:
1. Покращити промпти для пошуку категорій, щоб знаходити більше категорій
2. Створити систему оновлення категорій, яка:
   - Порівнює старі та нові категорії
   - Залишає знайдені категорії без змін
   - Позначає незнайдені категорії для ручної перевірки
   - Додає нові категорії
   - Показує статистику у спливаючому вікні

---

## [2025-01-21 22:45]

### Змінені файли:
- `app/templates/index.html`
- `app/static/main.js`
- `project_changes/CHANGELOG.md`

### Тип змін: added / updated

### Опис:
Додано кнопки очищення фільтрів для зручності використання:

**Кнопки очищення окремих фільтрів:**
- Додано кнопки очищення (×) біля кожного поля фільтру:
  - Пошук по назві
  - Конкурент
  - Категорія
  - Статус парсингу
  - Наявність
  - Ціна від
  - Ціна до
  - Проблемні товари
- Кнопки розміщені всередині полів (справа) з іконкою ×
- При натисканні очищається тільки відповідне поле

**Кнопка "Скинути фільтр":**
- Додано кнопку "Скинути фільтр" біля кнопки "Застосувати фільтр"
- При натисканні очищаються всі фільтри та завантажуються всі товари
- Кнопка має сірий колір для візуального розрізнення

**Нові функції:**
- `clearFilter(filterName)` - очищає конкретний фільтр
- `resetAllFilters()` - очищає всі фільтри та завантажує товари без фільтрів
- Додано обробники подій для всіх кнопок очищення

**UI покращення:**
- Поля з кнопками очищення мають `pr-10` (padding-right) для місця під кнопку
- Select поля мають `appearance-none` для правильного відображення кнопки
- Кнопки мають hover ефект та tooltip "Очистити"

### Причина змін:
Користувач попросив додати кнопки очищення біля кожного фільтру та кнопку "Скинути фільтр" для швидкого скидання всіх фільтрів.

---

## [2025-01-21 22:30]

### Змінені файли:
- `app/static/main.js`
- `project_changes/CHANGELOG.md`

### Тип змін: added / updated

### Опис:
Додано двостороннє автоматичне проставлення галочок між головними та підкатегоріями:

**Автоматичне вибирання підкатегорій (вниз):**
- При виборі головної категорії автоматично проставляються галочки у всіх підкатегоріях
- При знятті галочки з головної категорії автоматично знімаються галочки з усіх підкатегорій

**Автоматичне вибирання головної категорії (вгору):**
- При виборі всіх підкатегорій автоматично проставляється галочка в головній категорії
- При знятті галочки з будь-якої підкатегорії автоматично знімається галочка з головної категорії
- Працює рекурсивно для будь-якої глибини вкладеності

**Нові функції:**
- `getParentCategoryId()` - знаходить батьківську категорію для даної категорії
- `areAllSubcategoriesSelected()` - перевіряє, чи всі підкатегорії вибрані
- `updateParentCategoryState()` - оновлює стан батьківської категорії рекурсивно
- Оновлено `handleCategoryCheckboxChange()` для підтримки двостороннього оновлення

### Причина змін:
Користувач повідомив, що при виборі всіх підкатегорій автоматично має проставлятися галочка в головній категорії для зручності використання.

---

## [2025-01-21 22:15]

### Змінені файли:
- `app/static/main.js`
- `project_changes/CHANGELOG.md`

### Тип змін: added / updated

### Опис:
Додано автоматичне проставлення галочок у підкатегоріях при виборі головної категорії:

**Автоматичне вибирання підкатегорій:**
- При виборі головної категорії автоматично проставляються галочки у всіх підкатегоріях
- При знятті галочки з головної категорії автоматично знімаються галочки з усіх підкатегорій
- Додано функцію `getAllSubcategoryIds()` для рекурсивного пошуку всіх підкатегорій
- Додано функцію `handleCategoryCheckboxChange()` для обробки зміни checkbox з автоматичним оновленням підкатегорій
- Оновлюється як масив `selectedCategoryIds`, так і UI (галочки в checkbox)

### Причина змін:
Користувач повідомив, що при виборі головної категорії в фільтрі мають автоматично проставлятися галочки у всіх підкатегоріях для зручності використання.

---

## [2025-01-21 22:00]

### Змінені файли:
- `app/static/main.js`
- `app/templates/index.html`
- `app/main.py`
- `project_changes/CHANGELOG.md`

### Тип змін: fixed / bugfix

### Опис:
Виправлено дві критичні синтаксичні помилки JavaScript, які блокували весь код:

**Виправлення синтаксичних помилок:**
1. Виправлено помилку "Identifier 'tbody' has already been declared" в функції `loadProducts()`
   - Змінна `tbody` була оголошена тричі з `const` в одній функції (рядки 12, 57, 66)
   - Змінено на одну змінну `let tbody` на початку функції з перевіркою на існування

2. Виправлено помилку "Identifier 'currentTaskId' has already been declared"
   - Змінна `currentTaskId` була оголошена і в `main.js` (рядок 6), і в `tasks.js` (рядок 3)
   - Видалено дублікат з `main.js`, оскільки `tasks.js` завантажується першим
   - Додано перевірки `typeof currentTaskId !== 'undefined'` для безпечного використання
   - Ці помилки блокували виконання всього JavaScript коду, тому кнопки не працювали

**Додаткові виправлення:**
- Додано `type="button"` до кнопок в HTML для запобігання submit поведінки
- Використано клонування елементів для видалення старих обробників
- Додано подвійну прив'язку обробників (onclick + addEventListener)
- Створено функцію `initApp()` для перевірки наявності всіх елементів
- Додано затримку 100ms перед ініціалізацією для гарантії завантаження DOM

**Виправлення довгого завантаження товарів:**
- Додано таймаут 30 секунд для запиту до `/products/list`
- Покращено обробку помилок з детальними повідомленнями
- Додано перевірку існування елемента `productsTableBody` перед відображенням даних
- Додано обробку помилки `AbortError` (таймаут) з інформативним повідомленням

**Виправлення кнопки "Фільтри":**
- Додано `type="button"` до кнопки в HTML для запобігання submit поведінки
- Використано клонування елементів для видалення старих обробників
- Додано подвійну прив'язку обробників (onclick + addEventListener) з `stopImmediatePropagation()`
- Додано затримку 100ms перед ініціалізацією для гарантії завантаження DOM
- Створено функцію `initApp()` для перевірки наявності всіх елементів
- Додано повторну спробу ініціалізації через 1 секунду, якщо перша не вдалася

**Виправлення кнопки "Додати товар":**
- Додано `type="button"` до кнопки в HTML для запобігання submit поведінки
- Використано клонування елементів для видалення старих обробників
- Додано подвійну прив'язку обробників (onclick + addEventListener) з `stopImmediatePropagation()`
- Виправлено дублікати змінних `modal` та `addBtn` в коді
- Створено функцію `initApp()` для перевірки наявності всіх елементів

**Виправлення завантаження товарів:**
- Додано обробку помилок для фільтрації по категоріях в endpoint `/products/list`
- Додано try-catch блок для `load_competitors()` щоб уникнути зависання
- Покращено логування на етапі ініціалізації для виявлення проблем

**Покращення обробки помилок:**
- Додано перевірки існування елементів перед їх використанням
- Покращено повідомлення про помилки для користувача
- Додано логування на кожному етапі для дебагу

### Причина змін:
Користувач повідомив про три проблеми:
1. Підозріло довге завантаження товарів (показується "Завантаження...")
2. Кнопка "Фільтри" не відкриває вікно фільтру
3. Кнопка "Додати товар" перестала працювати

Всі три проблеми були виправлені шляхом покращення обробки помилок, додавання таймаутів та перевірок існування елементів.

---

## [2025-01-21 20:30]

### Змінені файли:
- `app/templates/index.html`
- `app/static/main.js`
- `app/main.py`
- `app/parser.py`
- `project_changes/CHANGELOG.md`

### Тип змін: updated / refactored

### Опис:
Оновлено дизайн та логіку фільтрів на сторінці "Товари" згідно з новими вимогами. Зміни:

**Панель фільтрів прихована за замовчуванням (index.html, main.js):**
- Додано кнопку "Фільтри" над таблицею для відкриття панелі
- Панель фільтрів прихована за замовчуванням (клас `hidden`)
- Додано кнопку "Закрити фільтр" всередині панелі
- Панель спроєктована у CRM-стилі: card-container, shadow-sm, rounded-xl
- Плавна анімація відкриття/закриття (transition-all duration-300)

**Кнопка "Застосувати фільтр" (index.html, main.js):**
- Додано кнопку "Застосувати фільтр" у нижній частині панелі фільтрів
- При натисканні виконується fetch GET /products?filters=...
- Таблиця оновлюється без перезавантаження сторінки
- Якщо знайдено ≥1 товар → показується кнопка "Парсити знайдене"
- Видалено автоматичне застосування фільтрів при зміні полів

**Фільтр "Конкурент" виправлено (main.js):**
- Підтягування списку конкурентів через API GET /competitors/list
- Select заповнюється правильно:
  - `<option value="all">Всі конкуренти</option>`
  - `<option value="{id}">{name}</option>`
- Значення "all" обробляється на backend

**Фільтр "Категорія" через модальне вікно (index.html, main.js):**
- Замінено select на кнопку "Обрати категорії"
- При натисканні відкривається модальне вікно з деревом категорій
- Дерево показує: Конкурент → Категорія → Підкатегорія
- Кожен елемент має checkbox
- Категорії розгортаються/згортаються при кліку на стрілку
- Після вибору категорій → кнопка "Додати в фільтр"
- Після вибору фільтр відображає: "N категорії вибрано"
- Формат даних для backend: `{"category_ids": ["cat312", "cat884", "cat112"]}`

**Модальне вікно вибору категорій (index.html, main.js):**
- Біла картка, rounded-xl, shadow-lg
- Дерево категорій з відступами для рівнів вкладеності
- Індикатори розгортання (стрілки) з плавною анімацією
- Кнопки: "Скасувати" (сіра) та "Додати в фільтр" (primary, синя)
- Максимальна висота з прокруткою (max-h-96 overflow-y-auto)

**Логіка "Парсити знайдене" (main.js):**
- Показується після застосування фільтрів, якщо результати > 0
- Викликає POST /tasks/parse_filtered з тими самими filters
- Вмикається прогрес-бар + блокування кнопок
- Після завершення показує toast: "Парсинг завершено успішно"
- Таблиця оновлюється з урахуванням фільтрів

**Checkbox біля товарів (index.html, main.js):**
- Додано checkbox у першу колонку таблиці
- Додано checkbox "Вибрати все" у заголовку таблиці
- Логіка select-all: ставить/знімає галочки у всіх рядках
- При виборі ≥1 товару показується кнопка "Парсити вибране"

**Кнопка "Парсити вибране" (main.js):**
- Показується при виборі ≥1 товару
- При натисканні: POST /tasks/parse_selected з масивом ID
- Прогрес-бар, блокування UI, оновлення таблиці після завершення

**Backend оновлення (main.py, parser.py):**
- Оновлено GET /products/list для підтримки `category_ids` (List[str])
- Фільтр по категоріях тепер працює з масивом ID категорій
- Оновлено `parse_filtered_products()` для підтримки `category_ids`
- Додано пошук назв категорій за ID у competitors.json для фільтрації

**Асинхронна логіка:**
- Усі кнопки запускають фонові задачі через tasks API
- UI використовує той же progress-bar, що й для парсингу товарів
- Блокування UI працює для всіх кнопок та фільтрів

### Причина змін:
Оновлення дизайну та логіки фільтрів для покращення користувацького досвіду. Панель фільтрів тепер прихована за замовчуванням, що економить місце на екрані. Модальне вікно для вибору категорій дозволяє зручно вибирати категорії з дерева. Кнопка "Застосувати фільтр" дає користувачу контроль над моментом застосування фільтрів.

---

## [2025-01-21 20:00]

### Змінені файли:
- `app/templates/index.html`
- `app/static/main.js`
- `app/static/tasks.js`
- `app/main.py`
- `app/parser.py`
- `project_changes/CHANGELOG.md`

### Тип змін: added / updated

### Опис:
Додано повну логіку фільтрів, вибору товарів та додаткових кнопок парсингу на сторінку "Товари". Зміни:

**Фільтр-блок над таблицею (index.html, main.js):**
- Додано блок фільтрів з полями:
  - Пошук по назві (input з debounce)
  - Конкурент (select з завантаженням з бази)
  - Категорія (select з завантаженням з усіх конкурентів)
  - Статус парсингу (select: Спарсено/Очікує/Помилка)
  - Наявність (select: В наявності/Немає в наявності)
  - Ціна від/до (2 number inputs)
  - Проблемні товари (checkbox - товари з помилками або без даних)
- При зміні будь-якого фільтра виконується AJAX запит GET /products?filters=...
- Таблиця оновлюється без перезавантаження сторінки

**Checkbox у таблиці (index.html, main.js):**
- Додано checkbox у першу колонку кожного рядка таблиці
- Додано checkbox "Вибрати все" у header таблиці
- При виборі 1+ товарів показується кнопка "Парсити вибране"
- Логіка "Вибрати все" ставить/знімає галочки з усіх товарів

**Кнопка "Парсити знайдене" (index.html, main.js, main.py, parser.py):**
- Кнопка прихована за замовчуванням
- Показується, якщо таблиця містить 1+ товарів (після фільтрації)
- При натисканні: POST /tasks/parse_filtered з фільтрами
- Запускає парсинг всіх відфільтрованих товарів у фоновому режимі
- Показує прогрес-бар та блокує UI під час виконання

**Кнопка "Парсити вибране" (index.html, main.js, main.py, parser.py):**
- Кнопка прихована за замовчуванням
- Показується при виборі 1+ товарів через checkbox
- При натисканні: POST /tasks/parse_selected з масивом ID товарів
- Запускає парсинг вибраних товарів у фоновому режимі
- Показує прогрес-бар та блокує UI під час виконання

**Backend endpoints (main.py):**
- Оновлено GET /products/list для підтримки фільтрів:
  - name, competitor_id, category, status, availability
  - price_from, price_to, problematic
- Додано POST /tasks/parse_filtered:
  - Приймає об'єкт filters
  - Створює фонову задачу parse_filtered_products
  - Повертає task_id для відстеження
- Додано POST /tasks/parse_selected:
  - Приймає об'єкт {"ids": ["id1", "id2", ...]}
  - Перевіряє існування всіх товарів
  - Створює фонову задачу parse_selected_products
  - Повертає task_id для відстеження

**Backend функції парсингу (parser.py):**
- Додано async def parse_filtered_products(task_id, filters):
  - Застосовує фільтри до списку товарів
  - Парсить всі відфільтровані товари у фоновому режимі
  - Оновлює прогрес задачі
  - Обробляє помилки для кожного товару
- Додано async def parse_selected_products(task_id, product_ids):
  - Парсить вибрані товари за ID
  - Перевіряє існування товарів
  - Оновлює прогрес задачі
  - Обробляє помилки для кожного товару

**Прогрес-бар та блокування UI (tasks.js):**
- Прогрес-бар вже був реалізований, додано підтримку нових типів задач
- Блокування UI працює для всіх кнопок, фільтрів та checkbox під час парсингу
- Після завершення задачі:
  - Оновлюється таблиця з поточними фільтрами
  - Скидаються всі checkbox
  - Ховаються кнопки "Парсити знайдене" та "Парсити вибране"
  - Показується toast повідомлення "Дані оновлено"
  - Розблоковується UI

**Завантаження даних для фільтрів (main.js):**
- Додано loadCompetitors() - завантажує список конкурентів для select
- Додано loadCategories() - збирає всі категорії з усіх конкурентів (рекурсивно)
- Категорії відображаються з повним шляхом (наприклад: "Догляд за садом > Дровоколи")

**Оптимізація запитів (main.js):**
- Додано debounce для полів пошуку та ціни (500ms)
- Зменшено кількість запитів до сервера при введенні тексту

**Оновлення після парсингу:**
- Після завершення задачі автоматично оновлюється таблиця
- Фільтри залишаються такими ж
- Галочки зникають
- Кнопки ховаються

### Причина змін:
Додавання повної системи фільтрації та вибіркового парсингу товарів для покращення користувацького досвіду. Тепер користувач може:
1. Фільтрувати товари за різними критеріями
2. Парсити тільки знайдені товари (відфільтровані)
3. Вибирати конкретні товари та парсити тільки їх
4. Бачити прогрес виконання в реальному часі
5. Працювати з інтерфейсом навіть під час парсингу (перегляд сторінок)

---

## [2025-01-21 19:30]

### Змінені файли:
- `app/gpt_client.py`
- `project_changes/CHANGELOG.md`

### Тип змін: updated / fixed

### Опис:
Покращено парсинг товарів з категорій для знаходження всіх товарів на сторінці. Зміни:

**Покращення оптимізації HTML для товарів (gpt_client.py):**
- Збільшено ліміти для збереження більшої кількості товарів:
  - Елементи з класами товарів: з 100 до 200 елементів кожного типу
  - Каруселі: з 50 до 100 елементів
  - Посилання на товари: з 200 до 500 посилань
  - JSON-LD скрипти: з 5 до 10
  - Елементи з data-атрибутами: з 100 до 200
- Додано збір контейнерів (div, article, li, section), що містять посилання та текст
- Збільшено загальний ліміт оптимізованого HTML з 60000 до 100000 символів
- Покращено фільтрацію посилань (виключено посилання на пагінацію, JavaScript тощо)
- Додано збереження JSON-LD даних (не видаляються при оптимізації)

**Покращення промпту для GPT:**
- Додано акцент на перевірку JSON-LD даних (там часто є повний список товарів)
- Додано інструкції для перевірки всіх контейнерів з посиланнями
- Покращено інструкції для визначення товарів (включно з CS-Cart класами)
- Додано зазначення, що краще знайти багато товарів (навіть з дублікатами), ніж пропустити їх

**Дедуплікація товарів:**
- Додано видалення дублікатів товарів за URL
- Нормалізація URL (видалення фрагментів, параметрів сортування)
- Логування кількості унікальних товарів після видалення дублікатів

**Логування:**
- Додано логування кількості знайдених важливих частин HTML
- Покращено логування процесу обробки товарів
- Додано інформацію про пропущені дублікати

### Причина змін:
1. Користувач повідомив, що знайдено лише 5 товарів замість більшої кількості на сторінці категорії
2. Потрібно було покращити алгоритм парсингу, щоб знаходити всі товари на сторінці
3. Потрібно було додати обробку JSON-LD даних, які часто містять повний список товарів
4. Потрібно було покращити фільтрацію та збір посилань на товари

---

## [2025-01-21 19:00]

### Змінені файли:
- `app/parser.py`
- `app/gpt_client.py`
- `project_changes/CHANGELOG.md`

### Тип змін: updated / fixed

### Опис:
Покращено обробку помилок при пошуку товарів у категоріях. Зміни:

**Покращення обробки помилок (parser.py):**
- Додано детальне логування помилок при парсингу категорій
- Помилки тепер не зупиняють обробку інших категорій - система продовжує обробку наступних категорій навіть якщо поточна не вдалася
- Покращено визначення фінального статусу задачі:
  - Якщо всі категорії з помилками → статус "failed"
  - Якщо є помилки, але є і успішні → статус "finished" з інформацією про помилки
  - Якщо всі успішні → статус "finished"
- Додано підсумкове логування з кількістю знайдених товарів, успішно оброблених категорій та помилок
- Покращено обробку помилок при запуску автоматичного парсингу нових товарів

**Покращення обробки помилок GPT (gpt_client.py):**
- Додано окрему обробку для помилок парсингу JSON від GPT (json.JSONDecodeError)
- Додано окрему обробку для помилок доступу до полів (KeyError)
- Додано логування відповіді GPT при помилках для дебагу
- Покращено повідомлення про помилки з деталями

**Логування:**
- Додано детальне логування на кожному етапі обробки категорій
- Логи допомагають визначити, на якому етапі виникає помилка
- Додано підсумкове логування зі статистикою

### Причина змін:
1. Користувач повідомив про помилку при пошуку товарів у категорії
2. Потрібно було покращити обробку помилок, щоб система продовжувала працювати навіть при помилках в окремих категоріях
3. Потрібно було додати детальне логування для дебагу проблем

---

## [2025-01-21 18:30]

### Змінені файли:
- `app/parser.py`
- `app/static/product.js`
- `project_changes/CHANGELOG.md`

### Тип змін: updated / fixed

### Опис:
Додано автоматичне визначення конкурента за URL товару та підстановку назви з бази даних. Зміни:

**Автоматичне визначення конкурента за URL (parser.py):**
- Додано функцію `find_competitor_by_url(product_url)` для визначення конкурента за доменом URL товару
- Функція порівнює домен URL товару з доменами URL конкурентів у базі даних
- Підтримує порівняння доменів з www та без www
- Після парсингу товару через GPT, система автоматично визначає конкурента за URL і підставляє його назву з бази даних
- Назва конкурента з HTML (як "Офіційний інтернет-магазин компанії Kentavr™") замінюється на назву з бази даних (як "Кентавр")
- Автоматично зберігається `competitor_id` при парсингу

**Покращення відображення назви конкурента (product.js):**
- При відображенні картки товару, система отримує актуальну назву конкурента з бази даних за `competitor_id`
- Якщо `competitor_id` є, використовується назва з бази даних замість назви з даних товару
- Це забезпечує консистентність назв конкурентів у всьому проєкті

**Логування:**
- Додано детальне логування для відстеження визначення конкурента за URL
- Логи допомагають дебагувати проблеми з визначенням конкурента

### Причина змін:
1. Користувач повідомив, що назва конкурента в картці товару має відповідати назві конкурента в картці конкурента
2. Парсер має аналізувати URL товару і визначати, який це конкурент, підставляючи правильну назву з бази даних
3. Назви типу "Офіційний інтернет-магазин компанії Kentavr™" мають замінюватися на короткі назви з бази даних (як "Кентавр")

---

## [2025-01-21 18:00]

### Змінені файли:
- `app/static/product.js`
- `app/main.py`
- `app/parser.py`
- `project_changes/CHANGELOG.md`

### Тип змін: updated / fixed

### Опис:
Виправлено та покращено клікабельні посилання у картці товару. Зміни:

**Покращення пошуку конкурента (product.js, main.py):**
- Покращено пошук конкурента за назвою: спочатку точний збіг, потім частковий
- Додано URL-декодування для правильного пошуку
- Додано детальне логування для дебагу пошуку конкурента та категорій
- `competitorName` тепер завжди використовує `competitor_id` з даних товару, якщо він є

**Збереження competitor_id (parser.py):**
- Додано збереження `competitor_id` при парсингу товарів (функція `save_result`)
- Тепер `competitor_id` зберігається при звичайному парсингу, а не тільки при discover_products

**Покращення breadcrumb категорій (product.js):**
- Додано детальне логування для дебагу пошуку категорій
- Всі категорії в breadcrumb тепер клікабельні (включаючи останню)
- Посилання на категорії використовують формат: `/competitors/{competitor_id}/category/{category_id}`

**Пояснення блоку "Правила парсингу":**
- Блок "Правила парсингу" був призначений для альтернативного методу парсингу через CSS селектори
- Оскільки основний парсер працює через GPT API, цей блок не використовується
- Блок вже приховано в коді (коментарі в product.js)

### Причина змін:
1. Користувач повідомив, що `competitorName` має містити назву конкурента зі списку конкурентів та бути клікабельним посиланням на картку конкурента
2. Користувач повідомив, що всі категорії в breadcrumb мають бути клікабельними посиланнями на картки категорій
3. Користувач запитав про призначення блоку "Правила парсингу"
4. Потрібно було забезпечити, щоб `competitor_id` зберігався при всіх типах парсингу

---

## [2025-01-21 17:30]

### Змінені файли:
- `app/templates/product.html`
- `app/static/product.js`
- `app/main.py`
- `project_changes/CHANGELOG.md`

### Тип змін: updated / refactored

### Опис:
Покращено клікабельні посилання у картці товару та прибрано непотрібний блок "Правила парсингу". Зміни:

**Покращення клікабельних посилань (product.js):**
- `competitorName` тепер використовує `competitor_id` з даних товару замість пошуку за назвою
- Додано fallback на пошук за назвою, якщо `competitor_id` відсутній
- Всі категорії в breadcrumb тепер клікабельні (включаючи останню категорію)
- Покращено пошук категорій за назвою з підтримкою часткового збігу
- Категорії тепер використовують `competitor_id` з даних товару для формування посилань

**API зміни (main.py):**
- Додано `competitor_id` до відповіді endpoint `GET /products/{product_id}` для покращення навігації

**UI зміни (product.html):**
- Прибрано блок "Правила парсингу" (parsing_rules), оскільки парсер працює через GPT
- Прибрано кнопки "Перегенерувати правила" та "Тестувати", які були пов'язані з CSS-селекторним парсингом
- Залишено тільки кнопки "Спарсити зараз" та "Парсинг всіх даних", які використовують GPT-парсер

**Frontend зміни (product.js):**
- Видалено обробники подій для видалених кнопок (regenerateRulesBtn, testRulesBtn)
- Видалено код відображення правил парсингу
- Покращено функцію `renderCategoryBreadcrumb()` для кращого пошуку категорій

### Причина змін:
1. Користувач повідомив, що `competitorName` має містити назву конкурента зі списку конкурентів та бути клікабельним
2. Користувач повідомив, що всі категорії в breadcrumb мають бути клікабельними (включаючи останню)
3. Користувач запитав про призначення блоку "Правила парсингу", який не використовується при GPT-парсингу
4. Спрощення UI та покращення навігації між сторінками

---

## [2025-01-21 16:00]

### Змінені файли:
- `app/templates/competitor.html`
- `app/static/competitor.js`
- `app/static/tasks.js`
- `app/gpt_client.py`
- `app/parser.py`
- `app/main.py`
- `project_changes/CHANGELOG.md`

### Тип змін: added / updated

### Опис:
Додано повну логіку "ВИБІР КАТЕГОРІЙ → ПОШУК ТОВАРІВ → АВТОДОДАВАННЯ У СИСТЕМУ". Зміни:

**UI зміни (competitor.html):**
- Додано чекбокси до кожної категорії в дереві категорій
- Додано кнопку "Знайти товари у вибраних категоріях" (id="discover-products-btn")
- Кнопка прихована за замовчуванням та показується при виборі хоча б однієї категорії
- Кнопка автоматично ховається після завершення пошуку товарів

**Backend зміни (gpt_client.py):**
- Додано метод `parse_category_products()` для парсингу товарів з категорії через GPT
- GPT аналізує сторінку категорії та витягує список товарів з полями: name, url, sku, price, availability
- Підтримка конвертації відносних URL в абсолютні

**Backend зміни (parser.py):**
- Додано функцію `discover_products()` для пошуку товарів у вибраних категоріях:
  - Для кожної вибраної категорії береться URL з competitor.categories
  - За допомогою GPT витягується список товарів категорії
  - Товари додаються у products.json з полями:
    - competitor_name
    - competitor_id
    - category_path
    - from_category_discovery = true
  - Після створення всіх товарів автоматично створюється друга фонова задача parse_newly_discovered_products()
- Додано функцію `parse_newly_discovered_products()` для автоматичного детального парсингу:
  - Збирає всі товари, у яких name або price = null та from_category_discovery = true
  - Проходить їх через звичайний GPT-парсер (parse_product_full)
  - Оновлює latest + history

**API зміни (main.py):**
- Додано endpoint `POST /tasks/discover_products`:
  - Request: `{"competitor_id": "...", "category_ids": ["id1","id2","id3"]}`
  - Створює task_id та запускає фонову функцію discover_products()
  - Повертає task_id для відстеження прогресу

**Frontend зміни (competitor.js):**
- Додано функцію `updateDiscoverButton()` для показу/приховування кнопки при зміні чекбоксів
- Додано функцію `discoverProducts()` для запуску пошуку товарів
- Додано обробник події для кнопки "Знайти товари у вибраних категоріях"
- Після завершення задачі автоматично скидаються всі чекбокси

**Frontend зміни (tasks.js):**
- Додано функцію `createDiscoverProductsTask()` для створення задачі на пошук товарів
- Оновлено `handleTaskComplete()` для спеціальної обробки задачі discover_products:
  - Показує повідомлення "Товари знайдено та додано"
  - Ховає кнопку "Знайти товари"
  - Скидає всі чекбокси
  - Оновлює список товарів на головній сторінці

**Прогрес та нотифікації:**
- discover_products пише у progress.json з такою ж структурою як інші задачі:
  - task_id, total, done, status, errors
- UI показує progress bar, статус та остаточне повідомлення
- Після завершення показується toast: "Товари знайдено та додано"

**Клікабельні поля у product.html:**
- competitor_name вже був клікабельним (посилання на /competitors/{id})
- category breadcrumb вже був клікабельним (посилання на /competitors/{competitor_id}/category/{category_id})
- Логіка вже була реалізована в product.js

**Автоматичне оновлення:**
- Після завершення задачі discover_products список товарів автоматично оновлюється (AJAX reload)
- Кнопка "Знайти товари" автоматично ховається
- Всі чекбокси автоматично скидаються

### Причина змін:
Додавання повної логіки автоматичного пошуку та додавання товарів з категорій конкурентів. Тепер користувач може вибрати категорії, знайти всі товари в них та автоматично додати їх у систему з подальшим детальним парсингом.

---

## [2025-01-21 15:30]

### Змінені файли:
- `app/static/competitors.js`
- `app/templates/competitors.html`
- `app/static/competitor.js`
- `app/static/tasks.js`
- `app/static/style.css`
- `app/parser.py`
- `project_changes/CHANGELOG.md`

### Тип змін: updated / bugfix

### Опис:
Виправлено проблеми з фоновим парсингом та блокуванням UI. Зміни:

**Виправлення парсингу категорій:**
- Оновлено функцію `parseCategories()` в `competitors.js` для використання фонових задач
- Додано підтримку `tasks.js` до сторінки списку конкурентів
- Додано глобальний індикатор та прогрес-бар на сторінку конкурентів
- Покращено обробку помилок у `parse_competitor_categories()` з детальним логуванням

**Виправлення блокування UI:**
- Посилання (тег `<a>`) тепер НЕ блокується під час парсингу
- Користувач може переглядати сторінки навіть під час парсингу
- Блокується тільки редагування (кнопки, інпути, селекти), а не навігація

**Виправлення парсингу всіх товарів:**
- Покращено обробку помилок у `parse_all_products()` з детальним логуванням
- Додано підрахунок успішних та невдалих операцій
- Задача завершується як "finished" навіть якщо є часткові помилки
- Покращено повідомлення про результати (показує кількість успішних та помилок)

**Покращення обробки помилок:**
- Додано очищення списку помилок при новому запуску задачі
- Додано детальне логування помилок з traceback
- Покращено відображення помилок у toast повідомленнях

### Причина змін:
Виправлення проблем, коли кнопки парсингу категорій не працювали, UI блокував навігацію, та покращення обробки помилок для кращого користувацького досвіду.

---

## [2025-01-21 15:00]

### Змінені файли:
- `app/parser.py`
- `app/main.py`
- `app/static/tasks.js` (створено)
- `app/static/main.js`
- `app/static/product.js`
- `app/static/competitor.js`
- `app/templates/index.html`
- `app/templates/product.html`
- `app/templates/competitor.html`
- `app/static/style.css`
- `project_changes/CHANGELOG.md`

### Тип змін: added / updated

### Опис:
Додано повну систему фонового парсингу з відстеженням прогресу та блокуванням UI. Зміни:

**Backend зміни:**
- Додано файл `app/db/progress.json` для зберігання прогресу задач
- Додано функції `load_progress()`, `save_progress()`, `update_task_progress()`, `get_task_status()` в `parser.py`
- Створено асинхронні функції фонового парсингу:
  - `parse_all_products(task_id)` - парсинг всіх товарів у фоновому режимі
  - `parse_single_product(task_id, product_id)` - парсинг одного товару у фоновому режимі
  - `parse_competitor_categories(task_id, competitor_id)` - парсинг категорій конкурента у фоновому режимі
- Додано API endpoints для роботи з задачами:
  - `POST /tasks/parse_products` - створення задачі на парсинг всіх товарів
  - `POST /tasks/parse_product/{product_id}` - створення задачі на парсинг одного товару
  - `POST /tasks/parse_categories/{competitor_id}` - створення задачі на парсинг категорій конкурента
  - `GET /tasks/status/{task_id}` - отримання статусу задачі (total, done, errors, status)
  - `GET /tasks/errors/{task_id}` - отримання списку помилок задачі
- Кожна задача має унікальний `task_id` (UUID4)
- Статуси задач: "running", "finished", "failed"
- Прогрес зберігається у файлі `/app/db/progress.json` з структурою:
  ```json
  {
    "tasks": {
      "task_id": {
        "type": "parse_products|parse_product|parse_categories",
        "total": 120,
        "done": 37,
        "errors": [],
        "status": "running|finished|failed"
      }
    }
  }
  ```

**Frontend зміни:**
- Створено `app/static/tasks.js` - система управління фоновим парсингом:
  - Функції створення задач (`createParseAllTask()`, `createParseProductTask()`, `createParseCategoriesTask()`)
  - Polling статусу задачі кожні 1.5 секунди
  - Управління прогрес-баром та глобальним індикатором
  - Toast повідомлення для успіху/помилок
  - Блокування/розблоковування UI під час парсингу
- Додано глобальний індикатор парсингу у шапці всіх сторінок:
  - Іконка з анімацією (spinner)
  - Текст "Парсер працює…"
- Додано локальний прогрес-бар на сторінках:
  - HTML структура з `task-progress`, `task-label`, `task-bar`
  - Відображення прогресу: "Виконано: X з Y (Z%)"
  - Зміна кольору в залежності від статусу (синій → зелений/червоний)
- Оновлено `main.js`:
  - Функції `parseProduct()` та `parseAll()` тепер використовують фонові задачі
  - Кнопки показують "В процесі…" під час роботи
- Оновлено `product.js`:
  - Функція `parseNow()` використовує фонову задачу
  - Функція `parseFull()` залишена синхронною (окрема операція)
- Оновлено `competitor.js`:
  - Функції парсингу категорій використовують фонові задачі
- Додано CSS для блокування UI:
  - Клас `.parsing-active` на `body` блокує редагування елементів
  - Кнопки, інпути, селекти стають неактивними (opacity: 0.5, pointer-events: none)
  - Анімація spinner для глобального індикатора

**UI покращення:**
- Toast повідомлення:
  - Успіх: "✅ Оновлення завершено\nДані успішно оновлені."
  - Помилка: "❌ Парсинг завершено з помилками\n[деталі помилок]"
  - Автоматичне закриття через 5 секунд
  - Підтримка багаторядкових повідомлень
- Прогрес-бар:
  - Плавні переходи (transition-all duration-300)
  - Зміна кольору: синій (running) → зелений (finished) → червоний (failed)
  - Автоматичне приховування через 3 секунди після завершення
- Блокування UI:
  - Тільки редагування елементів блокується, не вся сторінка
  - Можна переглядати дані, але не можна редагувати
  - Автоматичне розблоковування після завершення задачі

**Інтеграція:**
- Всі кнопки "Спарсити все", "Спарсити товар", "Спарсити категорії" тепер запускають фонові задачі
- Прогрес відстежується в реальному часі
- Після завершення задачі автоматично оновлюються дані на сторінці
- Помилки зберігаються та відображаються користувачу

### Причина змін:
Додавання повної системи фонового парсингу для покращення користувацького досвіду. Тепер парсинг не блокує UI, користувач бачить прогрес у реальному часі, а система автоматично обробляє помилки та повідомляє про результати.

---

## [2025-01-21 13:00]

### Змінені файли:
- `app/gpt_client.py`
- `app/static/competitor.js`
- `project_changes/CHANGELOG.md`

### Тип змін: updated

### Опис:
Покращено парсинг категорій конкурентів та відображення URL. Зміни:

**Покращення парсингу категорій:**
- Значно розширено промпт GPT для пошуку ВСІХ категорій у різних місцях сторінки:
  - Header навігація (головне меню)
  - Footer (нижня частина сторінки)
  - Sidebar (бокова панель)
  - Мобільне меню
  - Dropdown меню та підкатегорії
  - Каталог товарів
  - Всі блоки з посиланнями
  - Sitemap
- Додано детальні інструкції для GPT про обов'язковість знаходження ВСІХ категорій
- Збільшено розмір HTML контенту для аналізу з 30000 до 50000 символів (спеціальна функція `_optimize_html_for_categories`)
- Додано обробку відносних URL - автоматична конвертація в абсолютні URL
- Додано автоматичну генерацію ID для категорій на основі URL або назви (якщо ID відсутній)

**Виправлення відображення URL:**
- У дереві категорій тепер відображається реальний URL категорії замість "[URL]"
- URL відображається як посилання з обмеженням довжини (truncate)
- Додано tooltip з повним URL при наведенні
- Якщо URL відсутній, показується "[URL відсутній]"

**Технічні покращення:**
- Додано функцію `_optimize_html_for_categories()` для спеціальної оптимізації HTML при парсингу категорій
- Додано функцію `process_category()` для обробки категорій (конвертація URL, генерація ID)
- Покращено логування - додано інформацію про кількість знайдених категорій

### Причина змін:
Покращення точності парсингу категорій - тепер GPT знаходить більше категорій, перевіряючи всі можливі місця на сторінці. Також виправлено відображення URL для кращого UX.

---

## [2025-01-21 13:00]

### Змінені файли:
- `app/gpt_client.py`
- `app/static/competitor.js`
- `project_changes/CHANGELOG.md`

### Тип змін: updated / improved

### Опис:
Покращено парсинг категорій конкурентів та відображення URL. Зміни:

**Покращення парсингу категорій:**
- Значно покращено метод `_optimize_html_for_categories()` для кращого витягування категорій
- Тепер спочатку витягуються важливі частини сторінки (header, nav, footer, sidebar, всі посилання)
- Використовується BeautifulSoup для структурованого парсингу HTML
- Збільшено ліміт контенту до 60000 символів для кращого покриття
- Покращено промпт GPT з детальними інструкціями для пошуку ВСІХ категорій
- Додано перевірку всіх можливих місць: header, footer, sidebar, dropdown меню, mega-menu
- Покращено обробку відносних URL - тепер правильно конвертуються в абсолютні

**Виправлення відображення URL:**
- Виправлено відображення URL категорій у дереві - тепер показується повний URL замість "[URL]"
- URL тепер відображається як клікабельне посилання з правильним стилем
- Додано обрізання довгих URL для кращого відображення
- Покращено обробку відсутніх URL

**Покращення промптів GPT:**
- Додано детальні інструкції для пошуку всіх рівнів вкладеності (внучата, правнуки)
- Підкреслено важливість знаходження ВСІХ категорій без винятків
- Додано перевірку всіх <a> тегів та <ul>/<li> елементів
- Покращено інструкції щодо створення правильних абсолютних URL

### Причина змін:
Покращення якості парсингу категорій - тепер знаходяться всі категорії, а не тільки основні. Виправлення відображення URL категорій для кращого користувацького досвіду.

---

## [2025-01-21 12:00]

### Змінені файли:
- `app/models.py`
- `app/main.py`
- `app/parser.py`
- `app/gpt_client.py`
- `app/templates/index.html`
- `app/templates/product.html`
- `app/templates/settings.html`
- `app/templates/competitors.html` (створено)
- `app/templates/competitor.html` (створено)
- `app/templates/category.html` (створено)
- `app/static/competitors.js` (створено)
- `app/static/competitor.js` (створено)
- `app/static/category.js` (створено)
- `app/static/product.js`
- `project_changes/CHANGELOG.md`

### Тип змін: added

### Опис:
Додано повну функціональність для управління конкурентами та їх категоріями. Зміни:

**Нова вкладка в навігації:**
- Додано вкладку "Конкуренти" у всі HTML шаблони (index.html, product.html, settings.html)
- Навігація тепер: [Товари] [Налаштування] [Конкуренти]

**Нові моделі даних:**
- Додано модель `Category` для представлення категорій конкурентів (рекурсивна структура)
- Додано модель `Competitor` для представлення конкурентів
- Додано модель `CompetitorAdd` для додавання нових конкурентів

**Backend зміни:**
- Додано функції `load_competitors()` та `save_competitors()` в `parser.py` для роботи з `competitors.json`
- Додано метод `parse_competitor_categories()` в `GPTClient` для парсингу категорій через GPT
- Додано API endpoints:
  - `POST /competitors/add` - додавання нового конкурента
  - `GET /competitors/list` - отримання списку всіх конкурентів
  - `GET /competitors/{competitor_id}` - отримання деталей конкурента
  - `DELETE /competitors/{competitor_id}` - видалення конкурента
  - `POST /competitors/{competitor_id}/parse_categories` - парсинг категорій через GPT
  - `GET /competitors/by_name/{competitor_name}` - пошук конкурента за назвою
  - `GET /competitor/{competitor_id}` - HTML сторінка конкурента
  - `GET /competitors/{competitor_id}/category/{category_id}` - HTML сторінка категорії
  - `GET /competitors/{competitor_id}/category/{category_id}/data` - дані категорії

**Сторінка списку конкурентів (/competitors):**
- Таблиця конкурентів з колонками: Назва, URL, Категорій, Останнє оновлення, Статус, Дії
- Кнопка "Додати конкурента" для відкриття модального вікна
- Дії для кожного конкурента:
  - "Спарсити категорії" - запускає парсинг категорій через GPT
  - "Переглянути" - відкриває сторінку конкурента
  - "Видалити" - видаляє конкурента
- Модальне вікно для додавання конкурента з полями: назва, URL, примітка (опціонально)

**Сторінка конкурента (/competitor/{id}):**
- Відображення інформації про конкурента (назва, URL, останнє оновлення, примітки)
- Кнопки "Спарсити категорії" та "Оновити категорії"
- Collapsible дерево категорій з клікабельними посиланнями
- Кожна категорія має посилання на свою сторінку
- Відображення URL кожної категорії

**Сторінка категорії (/competitors/{id}/category/{category_id}):**
- Відображення назви та URL категорії
- Breadcrumb навігація: Конкурент → Категорія → Підкатегорія
- Відображення кількості підкатегорій
- Кнопка "Оновити категорію" (оновлює всі категорії конкурента)

**Парсинг категорій через GPT:**
- GPT аналізує HTML головної сторінки конкурента
- Шукає меню навігації, sitemap, структуру DOM
- Створює дерево категорій у форматі JSON з полями: id, name, url, children
- Зберігає структуру категорій у `competitors.json`
- Підтримує багаторівневу ієрархію категорій

**Оновлення сторінки товару (product.html):**
- Конкурент тепер клікабельний (посилання на сторінку конкурента)
- Категорії у breadcrumb тепер клікабельні (посилання на відповідні категорії конкурента)
- Автоматичний пошук конкурента за назвою для створення посилань
- Рекурсивний пошук категорій у дереві для створення правильних посилань

**Файл зберігання даних:**
- Створено `competitors.json` для зберігання даних про конкурентів
- Структура: `{"competitors": [{"id": "...", "name": "...", "url": "...", "categories": [...], ...}]}`

**Стиль та дизайн:**
- Всі нові сторінки використовують корпоративний CRM-стиль
- Спокійні кольори, багато повітря
- Картки, таблиці, кнопки у сучасному стилі
- Уніфікований дизайн з існуючими сторінками проєкту

**JavaScript файли:**
- `competitors.js` - логіка сторінки списку конкурентів
- `competitor.js` - логіка сторінки конкурента з деревом категорій
- `category.js` - логіка сторінки категорії
- Оновлено `product.js` для підтримки клікабельних посилань

### Причина змін:
Додавання повноцінної системи управління конкурентами та їх категоріями для аналізу структури інтернет-магазинів та відстеження категорій товарів. Це дозволяє краще орієнтуватися в структурі конкурентів та швидко переходити до потрібних категорій.

---

## [2025-01-20 19:00]

### Змінені файли:
- `app/templates/index.html`
- `app/templates/product.html`
- `app/templates/settings.html`
- `app/static/main.js`
- `app/static/product.js`
- `app/static/settings.js`
- `project_changes/CHANGELOG.md`

### Тип змін: updated / refactored

### Опис:
Повністю перероблено дизайн frontend проєкту у стилі корпоративної CRM-системи, схожої на CS-Cart. Зміни:

**Нова кольорова палітра (CRM-стиль):**
- Фон сторінки: #f3f4f6 (дуже світло-сірий)
- Картки/панелі: #ffffff з border border-gray-200
- Основний текст: #1f2937 (темно-сірий)
- Вторинний текст: #6b7280
- Межі: #e5e7eb
- Акцент (кнопки): #3b82f6 (спокійний синій)
- Success: #10b981 (м'який зелений)
- Warning: #f59e0b (без яскравості)
- Danger: #ef4444
- Всі кольори приглушені, без високої насиченості

**Стиль карток:**
- Уніфікований стиль: `bg-white shadow-sm rounded-xl p-6 border border-gray-200`
- Відступи між секціями: 24-32px
- Уникнуто яскравих кольорів у фонах

**Стиль кнопок (CRM-орієнтований):**
- Основна: `bg-blue-600 hover:bg-blue-700 text-white`
- Вторинна: `bg-gray-100 hover:bg-gray-200 text-gray-800 border border-gray-300`
- Дії: `bg-white hover:bg-gray-100 border border-gray-300 text-gray-600`
- Всі кнопки стримані та бізнес-орієнтовані

**Стиль бейджів:**
- Success: `bg-green-100 text-green-700 rounded-md px-2 py-1 text-sm`
- Danger: `bg-red-100 text-red-700`
- Neutral: `bg-gray-100 text-gray-600`
- Всі бейджі приглушені, без яскравих кольорів

**Таблиці (CRM-стиль):**
- Заголовки: `bg-gray-50 text-gray-600 uppercase text-xs tracking-wide`
- Рядки: `border-b border-gray-200 hover:bg-gray-50`
- Посилання: `text-blue-600 hover:underline`
- Дуже спокійний стиль без яскравих фонових кольорів

**Головна сторінка (index.html):**
- Header з сірим фоном (без яскравого синього)
- Вкладки у стилі: `bg-white border rounded-lg px-4 py-2 shadow-sm`
- Заголовок сторінки "Товари" з описом
- Панель дій над таблицею з стриманими кнопками
- Таблиця у чистому CRM-стилі з сірою шапкою та тонкими бордерами

**Сторінка товару (product.html):**
- Заголовок товару: `text-2xl font-semibold text-gray-800`
- SKU та інші дані: `text-gray-500 text-sm`
- Breadcrumb категорій у стилі CS-Cart: `text-gray-600 text-sm` з розділювачем " / "
- Блок "Поточні дані" у картці з правильними стилями
- Наявність у вигляді строгих бейджів (зелений/червоний)
- Графік історії в окремій картці зі стриманим фоном
- Кнопки управління мінімалістичні (primary → синій, secondary → сірий)
- JSON правил парсингу: `bg-gray-50 border border-gray-200`
- Логи у форматі таблиці з приглушеним текстом

**Сторінка налаштувань (settings.html):**
- Заголовок сторінки "Налаштування API" з описом
- Форма додавання ключа у картці з сучасними інпутами
- Таблиця ключів у CRM-стилі
- Статуси ключів у вигляді приглушених бейджів
- Кнопки "Активувати" (зелена) та "Видалити" (червона) у стриманому стилі

**JavaScript оновлення:**
- `main.js`: оновлено бейджі статусів у приглушених кольорах, таблиці з правильними стилями
- `product.js`: оновлено breadcrumb категорій у стилі CS-Cart, бейджі наявності, таблиці історії та логів
- `settings.js`: оновлено бейджі статусів та кнопки у стриманому стилі

**Уніфікація:**
- Всі сторінки мають однаковий фон (#f3f4f6)
- Однакові картки з border border-gray-200
- Однакові кнопки у стриманому стилі
- Однакові таблиці з сірими заголовками
- Однакова типографіка (font-sans, font-semibold)
- Усі елементи виглядають як частина єдиної UI-дизайн-системи

**Результат:**
- Інтерфейс виглядає стримано, корпоративно та професійно
- Схожість на CS-Cart, CRM та ERP-системи
- Відсутність зайвих кольорів та яскравих виділень
- Уніфікована дизайн-система для всіх сторінок
- Професійний бізнес-орієнтований вигляд

### Причина змін:
Перехід на корпоративний CRM-стиль для створення спокійного, професійного інтерфейсу, схожого на CS-Cart та інші CRM-системи. Стримані кольори та уніфікована дизайн-система покращують користувацький досвід для бізнес-користувачів.

---

## [2025-01-20 18:00]

### Змінені файли:
- `app/templates/index.html`
- `app/templates/product.html`
- `app/templates/settings.html`
- `app/static/style.css`
- `app/static/main.js`
- `app/static/product.js`
- `app/static/settings.js`
- `project_changes/CHANGELOG.md`

### Тип змін: updated / refactored

### Опис:
Повністю оновлено дизайн frontend проєкту до сучасного, чистого, мінімалістичного dashboard-стилю. Зміни:

**Дизайн-система:**
- Додано TailwindCSS через CDN для всіх сторінок
- Встановлено уніфіковану кольорову палітру:
  - Фон сторінки: #f7f8fa
  - Картки: #ffffff
  - Основний текст: #1e1e1e
  - Вторинний текст: #6b7280
  - Акцент: #3b82f6 (синій)
  - Зелений статус: #10b981
  - Помаранчевий для кнопок: #f59e0b (amber-500)
- Округлення компонентів: rounded-xl для карток, rounded-lg для кнопок
- Тіні: shadow-md для карток, shadow-sm для кнопок
- Сучасна типографіка: font-sans, font-semibold для заголовків

**Головна сторінка (index.html):**
- Оновлено header з великим лого та навігацією у стилі вкладок
- Додано картки-контейнери для таблиці та дій
- Оновлено кнопки: "Додати товар" (синя), "Спарсити все" (зелена)
- Таблиця з сучасним стилем: bg-gray-100 для заголовків, hover:bg-gray-50 для рядків
- Статуси у вигляді бейджів:
  - "Спарсено" → зелений бейдж (text-green-700 bg-green-100)
  - "Помилка" → червоний бейдж (text-red-700 bg-red-100)
  - "Очікує" → сірий бейдж (text-gray-700 bg-gray-100)
- URL товару стилізовано як посилання (text-blue-600 hover:underline)
- Кнопка "Спарсити цей товар" → amber-500 з hover-ефектом
- Модальне вікно з сучасним дизайном та backdrop-filter

**Сторінка товару (product.html):**
- Уніфіковано header з головною сторінкою
- Оновлено заголовок товару: text-2xl font-semibold
- Breadcrumb категорій з правильним стилем (останній елемент виділений)
- Блок актуальних даних у вигляді картки з flex-розміщенням
- Наявність у вигляді бейджів (зелений/червоний/жовтий)
- Графік історії в картці зі світлим фоном
- Таблиці історії та логів з сучасним тонким стилем (без border, з hover-ефектами)
- Блок правил парсингу з моноширинним шрифтом та bg-gray-100
- Кнопки управління з правильними кольорами:
  - "Спарсити зараз" → синя (#3b82f6)
  - "Перегенерувати правила" → фіолетова (purple-500)
  - "Тестувати" → сіра (gray-500)
  - "Парсинг всіх даних" → фіолетова (purple-500)

**Сторінка налаштувань (settings.html):**
- Уніфіковано header з іншими сторінками
- Форма додавання ключа в картці з сучасними інпутами
- Таблиця ключів з новим стилем
- Статуси ключів у вигляді бейджів (активний/неактивний)
- Статистика токенів з покращеним відображенням
- Кнопки "Активувати" (зелена) та "Видалити" (червона) з сучасним стилем

**JavaScript оновлення:**
- `main.js`: оновлено відображення товарів з новими бейджами статусів та стилями
- `product.js`: оновлено breadcrumb категорій, таблиці історії та логів, бейджі наявності
- `settings.js`: оновлено відображення ключів з новими бейджами та кнопками

**CSS оновлення:**
- Спрощено `style.css` - залишено тільки стилі для модальних вікон та утиліти
- Додано backdrop-filter для модальних вікон
- Покращено переходи для інтерактивних елементів

**Уніфікація:**
- Всі сторінки мають однакові відступи (px-6 py-8)
- Однакова ширина контенту (max-w-5xl mx-auto)
- Однакові картки (bg-white rounded-xl shadow-md p-6)
- Однакові кнопки (rounded-lg px-4 py-2 shadow-sm font-medium)
- Однакові таблиці (bg-gray-100 для заголовків, hover:bg-gray-50 для рядків)
- Однакова типографіка (font-sans, font-semibold)

**Результат:**
- Інтерфейс виглядає чистим, преміальним та сучасним
- Професійна dashboard-айдентика в стилі Notion/Linear/Vercel/Stripe
- Збалансовані кольори та відступи
- Покращена читабельність та UX

### Причина змін:
Оновлення дизайну frontend до сучасного dashboard-стилю для покращення користувацького досвіду та візуальної привабливості інтерфейсу. Уніфікація стилю всіх сторінок для консистентності та професійного вигляду.

---

## [2025-01-20 17:00]

### Змінені файли:
- `app/main.py`
- `app/parser.py`
- `project_changes/CHANGELOG.md`

### Тип змін: updated / bugfix

### Опис:
Виправлено проблему з оновленням даних після парсингу. Зміни:

**Виправлення оновлення даних:**
- Виправлено логіку збереження в `save_result()` - тепер правильно обробляються null значення
- Додано перезавантаження `db` після `save_result()` в endpoint `parse_full_product` для отримання оновлених даних
- Покращено логіку збереження `competitor_name` та `category_path` - зберігаються навіть якщо вони порожні
- Оновлено endpoint `get_product` для використання `name_parsed` замість `name`, якщо він є

**Результат:**
- Після натискання "Парсинг всіх даних" дані правильно оновлюються в картці товару
- SKU, конкурент та категорії тепер оновлюються після парсингу
- Всі дані правильно відображаються на сторінці товару

### Причина змін:
Виправлення проблеми, коли дані парсилися правильно, але не оновлювалися в інтерфейсі після парсингу. Проблема була в тому, що `db` не перезавантажувався після збереження даних.

---

## [2025-01-20 16:30]

### Змінені файли:
- `app/gpt_client.py`
- `app/parser.py`
- `project_changes/CHANGELOG.md`

### Тип змін: updated

### Опис:
Покращено парсинг артикулу, конкурента та категорій. Зміни:

**Покращення парсингу SKU/Артикулу:**
- Оновлено промпт GPT для кращого пошуку артикулу
- Додано інструкції для пошуку тексту "Артикул:" або "Артикул " перед числом
- GPT тепер шукає артикул у різних місцях: текст "Артикул: 123106" → "123106", атрибути data-sku, JSON-LD, мета-теги
- Покращено обробку артикулів - тепер правильно витягується число після "Артикул:"

**Додано парсинг конкурента:**
- Додано парсинг поля `competitor_name` (назва інтернет-магазину)
- GPT шукає назву конкурента в логотипі, мета-тегах (og:site_name), title сторінки, домені URL
- Дані автоматично зберігаються при повному парсингу

**Додано парсинг категорій:**
- Додано парсинг поля `category_path` (breadcrumb навігація)
- GPT шукає категорії в елементах з класами "breadcrumb", "breadcrumbs", JSON-LD
- Повертається як масив рядків, наприклад: ["Головна", "Електроніка", "Смартфони"]

**Backend зміни:**
- Оновлено `parse_product_full()` для повернення `competitor_name` та `category_path`
- Оновлено `parse_product()` для повернення цих полів при першому парсингу
- Оновлено `save_result()` для збереження `competitor_name` та `category_path` в базу даних
- Оновлено промпт GPT для включення всіх нових полів у формат JSON відповіді

**Результат:**
- Артикул тепер правильно парситься з тексту "Артикул: 123106"
- Конкурент та категорії автоматично визначаються та зберігаються
- При натисканні "Парсинг всіх даних" оновлюються всі поля, включаючи конкурента та категорії

### Причина змін:
Виправлення проблеми з неправильним парсингом артикулу та відсутністю даних про конкурента та категорії. GPT тепер отримує детальні інструкції для пошуку всіх необхідних даних.

---

## [2025-01-20 16:00]

### Змінені файли:
- `app/templates/product.html`
- `app/static/product.js`
- `app/main.py`
- `app/parser.py`
- `project_changes/CHANGELOG.md`

### Тип змін: added / updated

### Опис:
Додано кнопку "Парсинг всіх даних" для повного парсингу товарів. Зміни:

**Frontend зміни:**
- Додано нову кнопку "Парсинг всіх даних" біля кнопки "Спарсити зараз" в картці товару
- Кнопка має фіолетовий колір (bg-purple-500) для візуального розрізнення
- Додано обробник події `parseFull()` в `product.js` для виклику нового API endpoint

**Backend зміни:**
- Додано нову функцію `parse_product_full()` в `parser.py`, яка завжди виконує повний парсинг (як перший раз)
- Створено новий API endpoint `POST /products/parse_full/{product_id}` в `main.py`
- Endpoint виконує повний парсинг товару (назва, SKU, ціна, наявність) незалежно від того, чи товар вже був спарсений
- Додано логування операції `parse_full` з детальною інформацією про спарсені дані

**Функціонал:**
- Кнопка "Спарсити зараз" - парсить тільки ціну та наявність (якщо товар вже був спарсений)
- Кнопка "Парсинг всіх даних" - завжди парсить всі дані (назва, SKU, ціна, наявність), навіть якщо товар вже був спарсений
- Обидві операції логуються в історію товару з відповідними повідомленнями

### Причина змін:
Додано можливість виконати повний парсинг товару (з усіма даними) навіть після першого парсингу. Це корисно, коли потрібно оновити назву товару, SKU або перевірити правильність збережених даних.

---

## [2025-01-20 15:30]

### Змінені файли:
- `requirements.txt`
- `project_changes/CHANGELOG.md`

### Тип змін: updated

### Опис:
Виправлено помилку з встановленням залежностей. Зміни:

**Проблема:**
- `pydantic==2.5.0` потребував `pydantic-core==2.14.1`, який потребував компіляції з Rust
- Встановлення залежностей падало з помилкою через відсутність Rust toolchain

**Рішення:**
- Оновлено `requirements.txt` - замінено фіксовані версії на мінімальні версії з оператором `>=`
- Тепер використовуються новіші версії пакетів, які мають готові бінарники (wheels) для Windows
- `pydantic>=2.9.0` автоматично встановлює `pydantic-core` з готовими бінарниками

**Результат:**
- Всі залежності встановлюються успішно без необхідності компіляції
- Програма може бути запущена без додаткових налаштувань
- Сумісність з Python 3.14 забезпечена

### Причина змін:
Виправлення помилки встановлення залежностей, яка блокувала запуск програми. Оновлені версії пакетів мають готові бінарники для всіх платформ, що спрощує встановлення.

---

## [2025-12-07]

### Змінені файли:
- `app/models.py`
- `app/main.py`
- `app/gpt_client.py`
- `app/templates/product.html` (створено)
- `app/static/product.js` (створено)
- `app/static/main.js`
- `requirements.txt`
- `project_changes/CHANGELOG.md`

### Тип змін: added / updated

### Опис:
Створено сторінку "Картка товару" з повним функціоналом для детального перегляду та управління товарами. Зміни:

**Оновлення моделі Product:**
- Додано поля: `competitor_name`, `category_path`, `parsing_rules`, `logs`
- Розширено структуру даних для зберігання додаткової інформації про товар

**Backend зміни:**
- Додано API endpoint `GET /products/{id}` - отримання детальної інформації про товар
- Додано API endpoint `POST /products/regenerate_rules/{id}` - регенерація правил парсингу через GPT
- Додано API endpoint `POST /products/test_rules/{id}` - тестування правил парсингу на реальній сторінці
- Оновлено `POST /products/parse_one/{id}` - додано логування операцій парсингу
- Додано маршрут `GET /product/{id}` для відображення HTML сторінки картки товару
- Додано методи `generate_parsing_rules()` та `test_parsing_rules()` у `GPTClient` для роботи з правилами парсингу
- Додано залежність `beautifulsoup4` для парсингу HTML при тестуванні правил

**Frontend зміни:**
- Створено `product.html` з 7 блоками:
  1. Заголовок: назва, SKU, URL, конкурент, категорії (breadcrumb)
  2. Актуальні дані: ціна, наявність, дата оновлення
  3. Графік історії цін (Chart.js)
  4. Таблиця історії змін
  5. Парсинг та керування: правила парсингу, кнопки управління
  6. Результат тесту правил (динамічний блок)
  7. Логи парсингу
- Створено `product.js` для роботи з API, відображення графіків та обробки подій
- Додано посилання на картку товару в головній таблиці товарів
- Використано TailwindCSS для стилізації (dashboard-style дизайн)

**Функціонал:**
- Відображення всієї інформації про товар на одній сторінці
- Візуалізація історії цін у вигляді графіка
- Регенерація правил парсингу через GPT з автоматичним збереженням
- Тестування правил парсингу на реальній сторінці з відображенням результатів
- Логування всіх операцій (парсинг, регенерація правил, тестування)
- Кнопка "Спарсити зараз" для швидкого оновлення даних

### Причина змін:
Створення детальної сторінки для перегляду та управління товарами з можливістю тестування та регенерації правил парсингу. Це дозволяє користувачам краще контролювати процес парсингу та налаштовувати правила для кожного товару окремо.

---

## [2025-12-07]

### Змінені файли:
- `app/parser.py`
- `app/main.py`

### Тип змін: updated / refactored

### Опис:
Виправлено проблему з повільним завантаженням парсера. Зміни:

**Оптимізація продуктивності:**
- Переписано всі функції читання/запису файлів на асинхронні (використання `aiofiles`)
- `load_db()`, `save_db()`, `load_settings()`, `save_settings()` тепер асинхронні
- Усунено блокування event loop FastAPI при роботі з файлами
- Оптимізовано `parse_one_product()` - видалено зайві виклики `load_db()` (було 3 виклики, стало 1)
- Функція `update_history()` тепер приймає `db` як параметр, щоб уникнути подвійного читання файлу

**Покращення архітектури:**
- Всі ендпоінти в `main.py` оновлено для використання асинхронних функцій
- Покращено обробку помилок - не читаємо файл заново при помилках
- Зменшено кількість операцій I/O при парсингу товарів

**Результат:**
- Завантаження списку товарів працює значно швидше
- Парсинг товарів не блокує інші запити
- Покращена продуктивність завдяки асинхронному I/O
- Менше навантаження на файлову систему

### Причина змін:
Виправлення проблеми з підозріло довгим завантаженням парсера. Синхронне читання/запис файлів блокувало event loop FastAPI, що спричиняло повільну роботу. Перехід на асинхронне I/O вирішив цю проблему.

---

## [2025-12-07]

### Змінені файли:
- `app/gpt_client.py`
- `app/static/main.js`

### Тип змін: updated

### Опис:
Оптимізовано парсер для швидшого завантаження та покращено UX. Зміни:

**Оптимізація HTML контенту:**
- Додано функцію `_optimize_html()` для очищення HTML перед відправкою до GPT
- Видаляються скрипти, стилі та коментарі (не потрібні для парсингу)
- Зменшено розмір HTML контенту з 50000 до 25000-30000 символів (оптимально для GPT)
- JSON-LD дані зберігаються окремо та додаються до оптимізованого контенту
- Видаляються зайві пробіли та переноси рядків

**Покращення продуктивності:**
- Зменшено таймаут HTTP запитів з 30 до 20 секунд (швидше виявлення проблем)
- Додано таймаут 60 секунд для GPT API запитів
- Покращено логування для відстеження оптимізації HTML

**Покращення UX:**
- Додано індикатор прогресу при парсингу товару з етапами:
  - "Завантаження сторінки..."
  - "Аналіз HTML..."
  - "Обробка через GPT..."
  - "Збереження..."
- Відображається час виконання парсингу після завершення
- Покращено обробку помилок з детальними повідомленнями

**Результат:**
- Парсинг працює швидше завдяки оптимізації HTML
- Користувач бачить прогрес виконання замість просто "Парсинг..."
- Менше навантаження на GPT API через зменшений розмір контенту

### Причина змін:
Виправлення проблеми з повільним завантаженням парсера. Оптимізація HTML та додавання індикатора прогресу покращують швидкість та користувацький досвід.

---

## [2025-12-07]

### Змінені файли:
- `app/models.py`
- `app/gpt_client.py`
- `app/parser.py`
- `app/main.py`
- `app/templates/settings.html`
- `app/static/settings.js`
- `app/static/style.css`

### Тип змін: updated / added

### Опис:
Додано систему відстеження використання токенів для кожного API ключа. Зміни:

**Нові моделі:**
- Додано модель `TokenUsage` для зберігання інформації про використання токенів
- Оновлено модель `APIKey` - додано поле `token_usage_history` для історії використання

**Backend зміни:**
- Оновлено `gpt_client.py` - додано збереження інформації про токени з відповіді OpenAI API
- Оновлено `parser.py` - додано функції `save_token_usage()` та `get_token_statistics()` для збереження та отримання статистики токенів
- Додано API endpoint `/settings/token_stats/{key_id}` для отримання статистики токенів за період
- Покращено функції `load_settings()` та `save_settings()` для підтримки нових полів

**Frontend зміни:**
- Додано вибір періоду для відображення статистики токенів (Всі часи, Сьогодні, Останні 7 днів, Останні 30 днів)
- Додано колонку "Токени (за період)" у таблицю API ключів
- Відображення детальної інформації про токени: загальна кількість, кількість запитів, prompt та completion токени
- Додано стилі для красивого відображення інформації про токени

**Функціонал:**
- Автоматичне збереження використання токенів після кожного парсингу товару
- Можливість перегляду статистики токенів за різні періоди
- Детальна статистика: загальна кількість токенів, кількість запитів, розподіл на prompt та completion токени

### Причина змін:
Додано можливість відстеження використання токенів для кожного API ключа з можливістю вибору періоду для аналізу. Це дозволяє контролювати витрати на API та оптимізувати використання ключів.

---

## [2025-12-07]

### Змінені файли:
- `app/gpt_client.py`

### Тип змін: updated

### Опис:
Значно покращено парсинг ціни та наявності товарів. Зміни:

**Покращення промптів для GPT:**
- Додано детальні інструкції щодо пошуку ціни в різних місцях HTML (теги, атрибути, JSON-LD, мета-теги)
- Покращено інструкції щодо визначення наявності товару з логічними висновками
- Додано правило: якщо товар має ціну, але немає явного статусу наявності, вважати "в наявності"
- Збільшено розмір HTML контенту для аналізу з 30000 до 50000 символів

**Нові можливості:**
- Додано витягування JSON-LD структурованих даних з HTML (метод `_extract_json_ld`)
- JSON-LD дані тепер передаються GPT для кращого розпізнавання ціни та наявності
- Додано User-Agent заголовок для HTTP запитів (для кращої сумісності з сайтами)
- Покращено логування: додано детальне логування розпарсених даних

**Покращення обробки ціни:**
- Додано обробку ціни = 0 (тепер вважається null)
- Розширено список символів валюти для очищення (додано "грн.", "грн,")
- Покращено валідацію ціни: перевірка на > 0 перед збереженням

**Покращення визначення наявності:**
- Додано детальні інструкції для GPT щодо пошуку статусу наявності
- Додано логічні висновки: якщо є ціна → "в наявності", якщо немає ціни → "немає в наявності"
- Покращено розпізнавання різних форматів статусів наявності

### Причина змін:
Виправлення проблеми, коли парсер не знаходив ціну товару та неправильно визначав наявність, хоча на сайті товар був в наявності з ціною. GPT тепер отримує більш детальні інструкції та більше даних для аналізу, що має покращити точність парсингу.

---

## [2025-01-20]

### Створені файли:
- `requirements.txt` - залежності проєкту
- `README.md` - документація проєкту
- `.gitignore` - файл для ігнорування файлів у Git
- `run.py` - скрипт для зручного запуску сервера
- `app/__init__.py` - ініціалізація пакету
- `app/models.py` - моделі даних (Product, APIKey, Settings)
- `app/gpt_client.py` - клієнт для роботи з OpenAI API
- `app/parser.py` - логіка парсингу товарів
- `app/main.py` - FastAPI додаток з усіма ендпоінтами
- `app/templates/index.html` - головна сторінка з таблицею товарів
- `app/templates/settings.html` - сторінка налаштувань API ключів
- `app/static/style.css` - стилі для інтерфейсу
- `app/static/main.js` - JavaScript для головної сторінки
- `app/static/settings.js` - JavaScript для сторінки налаштувань
- `app/db.json` - база даних товарів (порожня)
- `app/settings.json` - налаштування API ключів (порожня)

### Тип змін: added

### Опис:
Створено повний проєкт "GPT Product Parser" з нуля. Проєкт включає:

**Backend (FastAPI):**
- Моделі даних для товарів, API ключів та налаштувань
- Клієнт для роботи з OpenAI API з підтримкою retry логіки
- Парсер товарів з автоматичним визначенням першого парсингу
- API ендпоінти для управління товарами та налаштуваннями
- Зберігання даних у JSON файлах

**Frontend:**
- Головна сторінка з таблицею товарів та можливістю додавання/парсингу
- Сторінка налаштувань для управління API ключами
- Модальне вікно для додавання товарів
- Сучасний мінімалістичний дизайн з використанням CSS

**Функціонал:**
- Додавання товарів з назвою та URL
- Парсинг товарів через GPT API (перший раз - повна інформація, наступні - тільки ціна та наявність)
- Управління API ключами (додавання, активація, видалення)
- Збереження історії змін цін та наявності
- Автоматичне визначення першого парсингу
- Обробка помилок та retry механізм

### Причина змін:
Створення нового проєкту згідно з вимогами користувача для парсингу товарів з інтернет-магазинів за допомогою GPT API.

---

## [2025-01-20]

### Змінені файли:
- `run.py`

### Тип змін: updated

### Опис:
Оновлено файл запуску `run.py` для автоматичного відкриття браузера після старту сервера. Додано:
- Використання модуля `webbrowser` для автоматичного відкриття браузера
- Затримка 2 секунди перед відкриттям (щоб сервер встиг запуститися)
- Запуск браузера в окремому потоці (daemon thread)
- Інформативні повідомлення в консолі

### Причина змін:
Покращення зручності використання - користувач не потребує вручну відкривати браузер.

---

## [2025-01-20]

### Створені файли:
- `run.bat`

### Тип змін: added

### Опис:
Створено `.bat` файл для Windows користувачів для зручного запуску програми подвійним кліком. Файл включає:
- Перевірку наявності Python
- Перевірку та автоматичне встановлення залежностей
- Підтримку віртуального середовища (якщо існує)
- Інформативні повідомлення українською мовою
- Обробку помилок з паузою перед закриттям

### Причина змін:
Покращення зручності використання на Windows - можливість запуску програми подвійним кліком без необхідності відкривати термінал.

---

## [2025-01-20]

### Змінені файли:
- `app/gpt_client.py`

### Тип змін: updated

### Опис:
Покращено парсинг ціни товарів. Зміни:
- Значно покращено промпти для GPT з детальними інструкціями щодо пошуку ціни
- Додано підтримку різних форматів цін (₴, грн, UAH, $, €, USD, EUR, руб, ₽)
- Покращено обробку ціни: видалення символів валюти, пробілів, конвертація ком у крапку
- Збільшено розмір HTML контенту для аналізу з 15000 до 30000 символів
- Додано логування для дебагу парсингу
- Ціна тепер може бути null (якщо не знайдена), але парсинг продовжується
- Покращено обробку помилок при парсингу JSON від GPT

### Причина змін:
Виправлення проблеми з незнаходженням ціни товарів під час парсингу. GPT тепер отримує більш детальні інструкції та більше контенту для аналізу.

---

